<!doctype html>
<html lang="hi">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Mera Pehla Website</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 2rem;
      background-color: #f5f5f5;
    }
    header {
      border-bottom: 2px solid #ddd;
      margin-bottom: 1rem;
    }
    h1 {
      color: #0078d7;
    }
  </style>
</head>
<body>
  <header>
    <h1>नमस्ते! मेरा पहला वेबसाइट badal  bhaynkar ❤</h1>
  </header>
  <p>यह मेरा पहला वेब पेज है जो मैंने बनाया है!</p>
  <p>आप भी वेबसाइट बना सकते हैं!</p>
</body>
</html><head>
  <title>My Website</title>

  <style>
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(to right, #74ebd5, #ACB6E5);
      color: #333;
      text-align: center;
      padding-top: 100px;
    }

    h1 {
      color: #0d47a1;
      font-size: 40px;
    }

    button {
      background-color: #0d47a1;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 10px;
      font-size: 18px;
      cursor: pointer;
    }
   </style>
</head>
<button>मुझे क्लिक करो</button>
<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>मेरा पहला वेबसाइट</title>
  <style>
    body {
      background: linear-gradient(to right, #74ebd5, #ACB6E5);
      font-family: 'Poppins', sans-serif;
      text-align: center;
      color: #222;
      margin-top: 50px;
    }

    h1 {
      color: darkblue;
      font-size: 2.5em;
    }

    p {
      font-size: 1.2em;
    }

    img {
      border-radius: 20px;
      margin-top: 20px;
      width: 250px;
    }

    button {
      margin-top: 25px;
      padding: 12px 25px;
      font-size: 1em;
      background-color: #007BFF;
      color: white;
      border: none;
      border-radius: 8px;
      cursor: pointer;
    }

    button:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <h1>नमस्ते! मेरा पहला वेबसाइट 💙</h1>
  <p>ये वेबसाइट मैंने खुद बनाई है!</p>

  <img src="https://picsum.photos/300" alt="सुंदर तस्वीर">

  <br>
  <button onclick="alert('शाबाश! आपने बटन क्लिक किया 😊')">मुझे क्लिक करो</button>
</body></html><!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Modern प्रोफेशनल वेबसाइट</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    *{box-sizing:border-box; margin:0; padding:0;}
    body {
      font-family: 'Poppins', sans-serif;
      color:#222;
      background: linear-gradient(-45deg,#74ebd5,#ACB6E5,#FFDEE9,#B5FFFC);
      background-size:400% 400%;
      animation: gradientBG 15s ease infinite;
      scroll-behavior: smooth;
    }
    @keyframes gradientBG{
      0%{background-position:0% 50%;}
      50%{background-position:100% 50%;}
      100%{background-position:0% 50%;}
    }

    /* Navbar */
    nav{
      display:flex;
      justify-content:space-between;
      align-items:center;
      padding:15px 30px;
      position:sticky;
      top:0;
      background:rgba(0,0,0,0.3);
      z-index:100;
    }
    nav .logo{
      font-size:1.5em;
      font-weight:bold;
      color:white;
    }
    nav ul{
      list-style:none;
      display:flex;
      gap:20px;
    }
    nav ul li a{
      text-decoration:none;
      color:white;
      font-weight:bold;
      padding:8px 12px;
      border-radius:8px;
      transition:0.3s;
    }
    nav ul li a:hover, nav ul li a.active{
      background:#FFD700;
      color:#222;
    }
    /* Hamburger */
    .hamburger{
      display:none;
      font-size:1.5em;
      color:white;
      cursor:pointer;
    }
    @media(max-width:768px){
      nav ul{
        display:none;
        flex-direction:column;
        background:rgba(0,0,0,0.8);
        position:absolute;
        top:60px;
        right:0;
        width:200px;
        padding:20px;
        border-radius:10px;
      }
      nav ul.show{
        display:flex;
      }
      .hamburger{
        display:block;
      }
    }

    /* Hero Banner */
    .hero{
      text-align:center;
      padding:100px 20px 50px 20px;
      position:relative;
      overflow:hidden;
    }
    .hero h1{
      font-size:3em;
      background:linear-gradient(90deg,red,orange,yellow,green,blue,violet);
      -webkit-background-clip:text;
      -webkit-text-fill-color:transparent;
      animation: rainbowText 5s linear infinite;
      margin-bottom:20px;
      opacity:0;
      transform:translateY(50px);
      animation: slideIn 1s forwards 0.5s;
    }
    @keyframes rainbowText{
      0%{background-position:0%;}
      100%{background-position:100%;}
    }
    @keyframes slideIn{
      to{opacity:1; transform:translateY(0);}
    }
    .hero p{
      font-size:1.2em;
      margin-bottom:30px;
      opacity:0;
      transform:translateY(50px);
      animation: slideIn 1s forwards 1s;
    }
    .hero button{
      margin:10px;
      padding:12px 25px;
      font-size:1em;
      border:none;
      border-radius:8px;
      cursor:pointer;
      background:#007BFF;
      color:white;
      transition:0.3s;
      opacity:0;
      transform:translateY(50px);
      animation: slideIn 1s forwards 1.5s;
    }
    .hero button:hover{
      transform:scale(1.1);
      background:#0056b3;
    }

    /* Sections */
    section{
      padding:60px 20px;
      opacity:0;
      transform:translateY(50px);
      transition:all 1s ease-out;
    }
    section.visible{
      opacity:1;
      transform:translateY(0);
    }

    /* Gallery */
    #gallery img{
      width:200px;
      margin:10px;
      border-radius:15px;
      cursor:pointer;
      transition:transform 0.3s, box-shadow 0.3s;
    }
    #gallery img:hover{
      transform:scale(1.1);
      box-shadow:0 8px 20px rgba(0,0,0,0.4);
    }

    /* Footer */
    footer{
      text-align:center;
      padding:20px;
      background:rgba(0,0,0,0.3);
      color:white;
    }
    .socials a{
      margin:0 10px;
      color:white;
      font-size:1.5em;
      transition:transform 0.3s,color 0.3s;
    }
    .socials a:hover{
      color:#FFD700;
      transform:scale(1.3);
    }

    /* Contact Modal */
    .modal{
      display:none;
      position:fixed;
      z-index:1000;
      left:0;
      top:0;
      width:100%;
      height:100%;
      background:rgba(0,0,0,0.7);
      justify-content:center;
      align-items:center;
    }
    .modal-content{
      background:white;
      padding:20px;
      border-radius:15px;
      max-width:400px;
      width:90%;
      text-align:center;
    }
    .modal input,.modal textarea{
      width:90%;
      padding:10px;
      margin:10px 0;
      border-radius:8px;
      border:1px solid #ccc;
    }
    .close-btn{
      background:red;
      color:white;
      border:none;
      padding:10px 20px;
      cursor:pointer;
      border-radius:8px;
    }
    .close-btn:hover{background:#cc0000;}
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav>
    <div class="logo">मेरीSite</div>
    <div class="hamburger"><i class="fas fa-bars"></i></div>
    <ul>
      <li><a href="#home" class="active">Home</a></li>
      <li><a href="#about">About</a></li>
      <li><a href="#gallery">Gallery</a></li>
      <li><a href="#contact" id="contactBtn">Contact</a></li>
    </ul>
  </nav>

  <!-- Hero -->
  <section class="hero" id="home">
    <h1>नमस्ते! मेरी Modern वेबसाइट 💙</h1>
    <p>ये वेबसाइट मैंने VS Code में बनाई है और ये इंटरैक्टिव है!</p>
    <button onclick="showMessage(1)">मुझे क्लिक करो</button>
    <button onclick="showMessage(2)">दूसरा बटन</button>
  </section>

  <!-- About -->
  <section id="about">
    <h2>About Me</h2>
    <p>मैं जूनियर वेब डेवलपर हूँ और HTML, CSS, JavaScript सीख रहा हूँ। मेरा लक्ष्य सुंदर और प्रोफेशनल वेबसाइट बनाना है।</p>
  </section>

  <!-- Gallery -->
  <section id="gallery">
    <h2>Gallery</h2>
    <img src="https://picsum.photos/200?5" alt="Image1">
    <img src="https://picsum.photos/200?6" alt="Image2">
    <img src="https://picsum.photos/200?7" alt="Image3">
  </section>

  <!-- Contact Modal -->
  <div id="contactModal" class="modal">
    <div class="modal-content">
      <h2>Contact Me</h2>
      <input type="text" placeholder="Your Name">
      <input type="email" placeholder="Your Email">
      <textarea rows="4" placeholder="Your Message"></textarea>
      <br>
      <button class="close-btn" id="closeModal">Close</button>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    © 2025 Modern वेबसाइट | Designed with ❤ by You
    <div class="socials">
      <a href="#"><i class="fab fa-facebook"></i></a>
      <a href="#"><i class="fab fa-twitter"></i></a>
      <a href="#"><i class="fab fa-instagram"></i></a>
    </div>
  </footer>

  <script>
    // Hamburger menu
    const hamburger = document.querySelector('.hamburger');
    const navUl = document.querySelector('nav ul');
    hamburger.addEventListener('click', ()=>{
      navUl.classList.toggle('show');
    });

    // Button messages
    function showMessage(num){
      alert(num===1?'शाबाश! आपने पहला बटन क्लिक किया 😊':'वाह! आपने दूसरा बटन क्लिक किया 😎');
    }

    // Image slider
    const slider = document.querySelector('#home img');
    const images=["https://picsum.photos/300?1","https://picsum.photos/300?2","https://picsum.photos/300?3","https://picsum.photos/300?4"];
    let index=0;
    if(slider){
      setInterval(()=>{
        slider.style.opacity=0;
        setTimeout(()=>{
          index=(index+1)%images.length;
          slider.src=images[index];
          slider.style.opacity=1;
        },500);
      },3000);
    }

    // Scroll reveal sections
    const sections = document.querySelectorAll('section');
    window.addEventListener('scroll',()=>{
      const scrollPos = window.scrollY + window.innerHeight - 100;
      sections.forEach(section=>{
        if(scrollPos>section.offsetTop){
          section.classList.add('visible');
        }
      });
    });

    // Contact modal
    const modal = document.getElementById('contactModal');
    const contactBtn = document.getElementById('contactBtn');
    const closeBtn = document.getElementById('closeModal');
    contactBtn.addEventListener('click',e=>{
      e.preventDefault();
      modal.style.display='flex';
    });
    closeBtn.addEventListener('click',()=>{modal.style.display='none';});
    window.addEventListener('click',e=>{if(e.target==modal) modal.style.display='none';});

    // Navbar active links
    const navLinks=document.querySelectorAll('nav ul li a');
    navLinks.forEach(link=>{
      link.addEventListener('click',()=>{
        navLinks.forEach(l=>l.classList.remove('active'));
        link.classList.add('active');
        if(navUl.classList.contains('show')) navUl.classList.remove('show');
      });
    });
  </script>
</body>
</html><!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Interactive Modern Website</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    *{box-sizing:border-box; margin:0; padding:0;}
    body{
      font-family:'Poppins',sans-serif;
      background:linear-gradient(-45deg,#74ebd5,#ACB6E5,#FFDEE9,#B5FFFC);
      background-size:400% 400%;
      animation: gradientBG 8s ease infinite;
      scroll-behavior:smooth;
      color:#222;
    }
    @keyframes gradientBG{
      0%{background-position:0% 50%;}
      50%{background-position:100% 50%;}
      100%{background-position:0% 50%;}
    }

    nav{
      display:flex;
      justify-content:space-between;
      align-items:center;
      padding:15px 30px;
      position:sticky;
      top:0;
      background:rgba(0,0,0,0.3);
      z-index:100;
    }
    nav .logo{font-size:1.5em;font-weight:bold;color:white;}
    nav ul{list-style:none;display:flex;gap:20px;}
    nav ul li a{text-decoration:none;color:white;font-weight:bold;padding:8px 12px;border-radius:8px;transition:0.3s;}
    nav ul li a:hover,nav ul li a.active{background:#FFD700;color:#222;}
    .hamburger{display:none;font-size:1.5em;color:white;cursor:pointer;}
    @media(max-width:768px){
      nav ul{display:none;flex-direction:column;background:rgba(0,0,0,0.8);position:absolute;top:60px;right:0;width:200px;padding:20px;border-radius:10px;}
      nav ul.show{display:flex;}
      .hamburger{display:block;}
    }

    /* Hero + Earnings */
    .hero-earnings{text-align:center;padding:60px 20px;overflow:hidden;}
    .hero-earnings h1{font-size:3em;background:linear-gradient(90deg,red,orange,yellow,green,blue,violet);-webkit-background-clip:text;-webkit-text-fill-color:transparent;animation:fastSlideIn 0.8s forwards;margin-bottom:15px;}
    .hero-earnings p{font-size:1.2em;margin-bottom:20px;animation:fastSlideIn 1s forwards 0.2s;}
    .hero-earnings button{margin:5px;padding:12px 25px;font-size:1em;border:none;border-radius:8px;cursor:pointer;transition:0.2s;animation:fastSlideIn 0.6s forwards 0.4s;}
    .hero-earnings button:hover{transform:scale(1.1);}
    #earnings{margin-top:30px;background:rgba(255,255,255,0.2);padding:25px;border-radius:20px;display:inline-block;box-shadow:0 5px 15px rgba(0,0,0,0.2);animation:fastSlideIn 0.8s forwards 0.7s;}

    @keyframes fastSlideIn{0%{opacity:0;transform:translateY(100px);}100%{opacity:1;transform:translateY(0);}}

    section{padding:60px 20px;opacity:0;transform:translateY(50px);transition:all 0.8s ease-out;}
    section.visible{opacity:1;transform:translateY(0);}

    /* Gallery */
    #gallery img{width:200px;margin:10px;border-radius:15px;cursor:pointer;transition:transform 0.3s, box-shadow 0.3s;}
    #gallery img:hover{transform:scale(1.1);box-shadow:0 8px 20px rgba(0,0,0,0.4);}

    /* Footer */
    footer{text-align:center;padding:20px;background:rgba(0,0,0,0.3);color:white;}
    .socials a{margin:0 10px;color:white;font-size:1.5em;transition:transform 0.3s,color 0.3s;}
    .socials a:hover{color:#FFD700;transform:scale(1.3);}

    /* Contact Modal */
    .modal{display:none;position:fixed;z-index:1000;left:0;top:0;width:100%;height:100%;background:rgba(0,0,0,0.7);justify-content:center;align-items:center;}
    .modal-content{background:white;padding:20px;border-radius:15px;max-width:400px;width:90%;text-align:center;}
    .modal input,.modal textarea{width:90%;padding:10px;margin:10px 0;border-radius:8px;border:1px solid #ccc;}
    .close-btn{background:red;color:white;border:none;padding:10px 20px;cursor:pointer;border-radius:8px;}
    .close-btn:hover{background:#cc0000;}
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav>
    <div class="logo">MySite</div>
    <div class="hamburger"><i class="fas fa-bars"></i></div>
    <ul>
      <li><a href="#home" class="active">Home</a></li>
      <li><a href="#about">About</a></li>
      <li><a href="#gallery">Gallery</a></li>
      <li><a href="#contact" id="contactBtn">Contact</a></li>
    </ul>
  </nav>

  <!-- Hero + Earnings -->
  <section class="hero-earnings" id="home">
    <h1>Hello! Welcome to My Modern Website 💙</h1>
    <p>This website is created with VS Code and is fully interactive!</p>
    <button onclick="showMessage(1)">Click Me</button>
    <button onclick="showMessage(2)">Second Button</button>

    <div id="earnings">
      <h2>Earn with This Website 💰</h2>
      <p>You can earn through Affiliate links or Donations.</p>
      <button onclick="window.open('https://www.example.com/affiliate','_blank')">Affiliate Link</button>
      <button onclick="window.open('https://www.paypal.com/donate?hosted_button_id=XXXX','_blank')">Donate / Tip</button>
      <div style="margin-top:15px;">
        <h3>Live Clicks: <span id="clickCounter">0</span></h3>
      </div>
    </div>
  </section>

  <!-- About -->
  <section id="about">
    <h2>About Me</h2>
    <p>I am a junior web developer learning HTML, CSS, and JavaScript. My goal is to create beautiful and professional websites.</p>
  </section>

  <!-- Gallery -->
  <section id="gallery">
    <h2>Gallery</h2>
    <img src="https://picsum.photos/200?5" alt="Image1">
    <img src="https://picsum.photos/200?6" alt="Image2">
    <img src="https://picsum.photos/200?7" alt="Image3">
  </section>

  <!-- Contact Modal -->
  <div id="contactModal" class="modal">
    <div class="modal-content">
      <h2>Contact Me</h2>
      <input type="text" placeholder="Your Name">
      <input type="email" placeholder="Your Email">
      <textarea rows="4" placeholder="Your Message"></textarea>
      <br>
      <button class="close-btn" id="closeModal">Close</button>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    © 2025 Modern Website | Designed with ❤ by You
    <div class="socials">
      <a href="#"><i class="fab fa-facebook"></i></a>
      <a href="#"><i class="fab fa-twitter"></i></a>
      <a href="#"><i class="fab fa-instagram"></i></a>
    </div>
  </footer>

  <script>
    // Hamburger menu
    const hamburger = document.querySelector('.hamburger');
    const navUl = document.querySelector('nav ul');
    hamburger.addEventListener('click', ()=>{
      navUl.classList.toggle('show');
    });

    // Button click messages + live counter
    let counter=0;
    function showMessage(num){
      alert(num===1?'Great! You clicked the first button 😊':'Wow! You clicked the second button 😎');
      counter++;
      document.getElementById('clickCounter').innerText = counter;
    }

    // Scroll reveal sections
    const sections=document.querySelectorAll('section');
    window.addEventListener('scroll',()=>{
      const scrollPos = window.scrollY + window.innerHeight - 100;
      sections.forEach(section=>{
        if(scrollPos>section.offsetTop){
          section.classList.add('visible');
        }
      });
    });

    // Contact modal
    const modal=document.getElementById('contactModal');
    const contactBtn=document.getElementById('contactBtn');
    const closeBtn=document.getElementById('closeModal');
    contactBtn.addEventListener('click',e=>{
      e.preventDefault();
      modal.style.display='flex';
    });
    closeBtn.addEventListener('click',()=>{modal.style.display='none';});
    window.addEventListener('click',e=>{if(e.target==modal) modal.style.display='none';});

    // Navbar active links
    const navLinks=document.querySelectorAll('nav ul li a');
    navLinks.forEach(link=>{
      link.addEventListener('click',()=>{
        navLinks.forEach(l=>l.classList.remove('active'));
        link.classList.add('active');
        if(navUl.classList.contains('show')) navUl.classList.remove('show');
      });
    });
  </script>
</body>
</html<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Interactive Modern Website</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    /* Basic Reset */
    *{box-sizing:border-box;margin:0;padding:0;}
    body{
      font-family:'Poppins',sans-serif;
      scroll-behavior:smooth;
      color:#222;
      background:linear-gradient(-45deg,#74ebd5,#ACB6E5,#FFDEE9,#B5FFFC);
      background-size:400% 400%;
      animation: gradientBG 10s ease infinite;
    }

    @keyframes gradientBG{
      0%{background-position:0% 50%;}
      50%{background-position:100% 50%;}
      100%{background-position:0% 50%;}
    }

    /* Navbar */
    nav{
      display:flex;
      justify-content:space-between;
      align-items:center;
      padding:15px 30px;
      position:sticky;
      top:0;
      background:rgba(0,0,0,0.3);
      z-index:100;
    }
    nav .logo{font-size:1.5em;font-weight:bold;color:white;}
    nav ul{list-style:none;display:flex;gap:20px;}
    nav ul li a{text-decoration:none;color:white;font-weight:bold;padding:8px 12px;border-radius:8px;transition:0.3s;}
    nav ul li a:hover,nav ul li a.active{background:#FFD700;color:#222;}
    .hamburger{display:none;font-size:1.5em;color:white;cursor:pointer;}
    @media(max-width:768px){
      nav ul{display:none;flex-direction:column;background:rgba(0,0,0,0.8);position:absolute;top:60px;right:0;width:200px;padding:20px;border-radius:10px;}
      nav ul.show{display:flex;}
      .hamburger{display:block;}
    }

    /* Hero Banner */
    .hero{
      text-align:center;
      padding:80px 20px;
      position:relative;
      overflow:hidden;
    }
    .hero h1{
      font-size:3em;
      background:linear-gradient(90deg,red,orange,yellow,green,blue,violet);
      -webkit-background-clip:text;
      -webkit-text-fill-color:transparent;
      animation: slideIn 1s forwards;
      margin-bottom:20px;
    }
    .hero p{
      font-size:1.2em;
      margin-bottom:30px;
      animation: slideIn 1.2s forwards;
    }
    .hero button{
      margin:5px;
      padding:12px 25px;
      font-size:1em;
      border:none;
      border-radius:8px;
      cursor:pointer;
      transition:0.3s;
      animation: slideIn 1.4s forwards;
    }
    .hero button:hover{
      transform:scale(1.1);
      box-shadow:0 5px 15px rgba(0,0,0,0.3);
    }

    @keyframes slideIn{0%{opacity:0;transform:translateY(100px);}100%{opacity:1;transform:translateY(0);}}

    /* Earnings Section */
    #earnings{
      margin:50px auto;
      max-width:700px;
      background:rgba(255,255,255,0.2);
      padding:30px;
      border-radius:20px;
      box-shadow:0 5px 15px rgba(0,0,0,0.2);
      text-align:center;
      transition:0.3s;
    }
    #earnings button{
      margin:10px;
      padding:12px 25px;
      font-size:1em;
      border:none;
      border-radius:8px;
      cursor:pointer;
      background:#28a745;
      color:white;
      transition:0.3s;
    }
    #earnings button:hover{
      background:#1e7e34;
      transform:scale(1.05);
    }

    /* About Section */
    #about{
      display:flex;
      flex-wrap:wrap;
      align-items:center;
      justify-content:center;
      gap:20px;
      padding:80px 20px;
    }
    #about img{width:300px;border-radius:15px;box-shadow:0 5px 15px rgba(0,0,0,0.2);}
    #about div{text-align:left;max-width:500px;}

    /* Gallery */
    #gallery{padding:60px 20px;text-align:center;}
    #gallery img{
      width:200px;
      margin:10px;
      border-radius:15px;
      cursor:pointer;
      transition:transform 0.3s, box-shadow 0.3s;
    }
    #gallery img:hover{transform:scale(1.1);box-shadow:0 8px 20px rgba(0,0,0,0.4);}

    /* Contact Modal */
    .modal{display:none;position:fixed;z-index:1000;left:0;top:0;width:100%;height:100%;background:rgba(0,0,0,0.7);justify-content:center;align-items:center;}
    .modal-content{background:white;padding:20px;border-radius:15px;max-width:400px;width:90%;text-align:center;}
    .modal input,.modal textarea{width:90%;padding:10px;margin:10px 0;border-radius:8px;border:1px solid #ccc;}
    .close-btn{background:red;color:white;border:none;padding:10px 20px;cursor:pointer;border-radius:8px;}
    .close-btn:hover{background:#cc0000;}

    /* Footer */
    footer{text-align:center;padding:20px;background:rgba(0,0,0,0.3);color:white;}
    .socials a{margin:0 10px;color:white;font-size:1.5em;transition:transform 0.3s,color 0.3s;}
    .socials a:hover{color:#FFD700;transform:scale(1.3);}

    /* Section animations */
    section{opacity:0;transform:translateY(50px);transition:all 0.8s ease-out;}
    section.visible{opacity:1;transform:translateY(0);}
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav>
    <div class="logo">MySite</div>
    <div class="hamburger"><i class="fas fa-bars"></i></div>
    <ul>
      <li><a href="#home" class="active">Home</a></li>
      <li><a href="#about">About</a></li>
      <li><a href="#gallery">Gallery</a></li>
      <li><a href="#contact" id="contactBtn">Contact</a></li>
    </ul>
  </nav>

  <!-- Hero Banner -->
  <section class="hero" id="home">
    <h1>Welcome to My Modern Website 💙</h1>
    <p>Interactive, fast, and monetizable website built with VS Code!</p>
    <button onclick="showMessage(1)">Click Me</button>
    <button onclick="showMessage(2)">Second Button</button>
  </section>

  <!-- Earnings Section -->
  <section id="earnings">
    <h2>Earn with This Website 💰</h2>
    <p>Use affiliate links or donations to earn!</p>
    <button onclick="window.open('https://www.example.com/affiliate','_blank')">Affiliate Link</button>
    <button onclick="window.open('https://www.paypal.com/donate?hosted_button_id=XXXX','_blank')">Donate / Tip</button>
    <div style="margin-top:15px;">
      <h3>Live Clicks: <span id="clickCounter">0</span></h3>
    </div>
  </section>

  <!-- About Section -->
  <section id="about">
    <img src="https://picsum.photos/300?8" alt="About Image">
    <div>
      <h2>About Me</h2>
      <p>I am a junior web developer learning HTML, CSS, and JavaScript. My goal is to create professional, interactive websites that are visually stunning and monetizable.</p>
    </div>
  </section>

  <!-- Gallery Section -->
  <section id="gallery">
    <h2>Gallery</h2>
    <img src="https://picsum.photos/200?5" alt="Image1">
    <img src="https://picsum.photos/200?6" alt="Image2">
    <img src="https://picsum.photos/200?7" alt="Image3">
  </section>

  <!-- Contact Modal -->
  <div id="contactModal" class="modal">
    <div class="modal-content">
      <h2>Contact Me</h2>
      <input type="text" placeholder="Your Name">
      <input type="email" placeholder="Your Email">
      <textarea rows="4" placeholder="Your Message"></textarea>
      <br>
      <button class="close-btn" id="closeModal">Close</button>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    © 2025 Modern Website | Designed with ❤ by You
    <div class="socials">
      <a href="#"><i class="fab fa-facebook"></i></a>
      <a href="#"><i class="fab fa-twitter"></i></a>
      <a href="#"><i class="fab fa-instagram"></i></a>
    </div>
  </footer>

  <script>
    // Hamburger menu
    const hamburger = document.querySelector('.hamburger');
    const navUl = document.querySelector('nav ul');
    hamburger.addEventListener('click', ()=>{navUl.classList.toggle('show');});

    // Button click messages + live counter
    let counter=0;
    function showMessage(num){
      alert(num===1?'Great! You clicked the first button 😊':'Wow! You clicked the second button 😎');
      counter++;
      document.getElementById('clickCounter').innerText = counter;
    }

    // Scroll reveal for sections
    const sections=document.querySelectorAll('section');
    window.addEventListener('scroll',()=>{
      const scrollPos=window.scrollY+window.innerHeight-100;
      sections.forEach(section=>{
        if(scrollPos>section.offsetTop){section.classList.add('visible');}
      });
    });

    // Contact modal
    const modal=document.getElementById('contactModal');
    const contactBtn=document.getElementById('contactBtn');
    const closeBtn=document.getElementById('closeModal');
    contactBtn.addEventListener('click',e=>{
      e.preventDefault(); modal.style.display='flex';
    });
    closeBtn.addEventListener('click',()=>{modal.style.display='none';<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Ultimate Beautiful Website</title>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
<style>
/* ---------------- Basic Reset ---------------- */
*{margin:0;padding:0;box-sizing:border-box;}
body{
  font-family:'Poppins',sans-serif;
  scroll-behavior:smooth;
  color:#222;
  background:linear-gradient(-45deg,#74ebd5,#ACB6E5,#FFDEE9,#B5FFFC);
  background-size:400% 400%;
  animation:gradientBG 12s ease infinite;
}
@keyframes gradientBG{0%{background-position:0% 50%;}50%{background-position:100% 50%;}100%{background-position:0% 50%;}}

/* ---------------- Navbar ---------------- */
nav{display:flex;justify-content:space-between;align-items:center;padding:15px 30px;position:sticky;top:0;background:rgba(0,0,0,0.3);z-index:100;}
nav .logo{font-size:1.5em;font-weight:bold;color:white;}
nav ul{list-style:none;display:flex;gap:20px;}
nav ul li a{text-decoration:none;color:white;font-weight:bold;padding:8px 12px;border-radius:8px;transition:0.3s;}
nav ul li a:hover, nav ul li a.active{background:#FFD700;color:#222;}
.hamburger{display:none;font-size:1.5em;color:white;cursor:pointer;}
@media(max-width:768px){nav ul{display:none;flex-direction:column;background:rgba(0,0,0,0.8);position:absolute;top:60px;right:0;width:200px;padding:20px;border-radius:10px;}nav ul.show{display:flex;}.hamburger{display:block;}}

/* ---------------- Hero ---------------- */
.hero{position:relative;text-align:center;padding:120px 20px;overflow:hidden;}
.hero h1{font-size:3em;background:linear-gradient(90deg,#ff6ec4,#7873f5,#42e695);-webkit-background-clip:text;-webkit-text-fill-color:transparent;animation:slideIn 1s forwards;margin-bottom:20px;}
.hero p{font-size:1.2em;margin-bottom:30px;animation:slideIn 1.2s forwards;}
.hero button{margin:5px;padding:12px 25px;font-size:1em;border:none;border-radius:8px;cursor:pointer;transition:0.3s;position:relative;overflow:hidden;}
.hero button::after{content:'';position:absolute;top:0;left:50%;width:0;height:100%;background:rgba(255,255,255,0.3);transition:0.5s;transform:translateX(-50%);border-radius:8px;}
.hero button:hover::after{width:100%;}
.hero button:hover{transform:scale(1.1);box-shadow:0 5px 15px rgba(0,0,0,0.3);}
@keyframes slideIn{0%{opacity:0;transform:translateY(100px);}100%{opacity:1;transform:translateY(0);}}

/* Floating particles */
.particle{position:absolute;width:10px;height:10px;background:rgba(255,255,255,0.7);border-radius:50%;animation:moveParticle linear infinite;}
@keyframes moveParticle{0%{transform:translateY(0);}100%{transform:translateY(-800px);}}

/* ---------------- Earnings ---------------- */
#earnings{margin:50px auto;max-width:700px;background:rgba(255,255,255,0.2);padding:30px;border-radius:20px;box-shadow:0 5px 15px rgba(0,0,0,0.2);text-align:center;transition:0.3s;}
#earnings button{margin:10px;padding:12px 25px;font-size:1em;border:none;border-radius:8px;cursor:pointer;background:#28a745;color:white;transition:0.3s;}
#earnings button:hover{background:#1e7e34;transform:scale(1.05);}

/* ---------------- About ---------------- */
#about{display:flex;flex-wrap:wrap;align-items:center;justify-content:center;gap:20px;padding:80px 20px;opacity:0;transform:translateY(50px);transition:all 0.8s ease-out;}
#about.visible{opacity:1;transform:translateY(0);}
#about img{width:300px;border-radius:15px;box-shadow:0 5px 15px rgba(0,0,0,0.2);}
#about div{text-align:left;max-width:500px;}

/* ---------------- Gallery ---------------- */
#gallery{padding:60px 20px;text-align:center;opacity:0;transform:translateY(50px);transition:all 0.8s ease-out;}
#gallery.visible{opacity:1;transform:translateY(0);}
#gallery img{width:200px;margin:10px;border-radius:15px;cursor:pointer;transition:transform 0.3s, box-shadow 0.3s;}
#gallery img:hover{transform:scale(1.1) rotate(2deg);box-shadow:0 8px 20px rgba(0,0,0,0.4);}

/* ---------------- Contact Modal ---------------- */
.modal{display:none;position:fixed;z-index:1000;left:0;top:0;width:100%;height:100%;background:rgba(0,0,0,0.7);justify-content:center;align-items:center;}
.modal-content{background:white;padding:20px;border-radius:15px;max-width:400px;width:90%;text-align:center;}
.modal input,.modal textarea{width:90%;padding:10px;margin:10px 0;border-radius:8px;border:1px solid #ccc;}
.close-btn{background:red;color:white;border:none;padding:10px 20px;cursor:pointer;border-radius:8px;}
.close-btn:hover{background:#cc0000;}

/* ---------------- Footer ---------------- */
footer{text-align:center;padding:20px;background:rgba(0,0,0,0.3);color:white;position:relative;}
.socials a{margin:0 10px;color:white;font-size:1.5em;transition:transform 0.3s,color 0.3s;}
.socials a:hover{color:#FFD700;transform:scale(1.3);}
#scrollTop{position:fixed;bottom:30px;right:30px;padding:10px 15px;background:#007bff;color:white;border:none;border-radius:50%;cursor:pointer;display:none;box-shadow:0 5px 10px rgba(0,0,0,0.3);}
#scrollTop:hover{background:#0056b3;}

/* Section Reveal */
section{opacity:0;transform:translateY(50px);transition:all 0.8s ease-out;}
section.visible{opacity:1;transform:translateY(0);}
</style>
</head>
<body>

<!-- Navbar -->
<nav>
  <div class="logo">MySite</div>
  <div class="hamburger"><i class="fas fa-bars"></i></div>
  <ul>
    <li><a href="#home" class="active">Home</a></li>
    <li><a href="#about">About</a></li>
    <li><a href="#gallery">Gallery</a></li>
    <li><a href="#contact" id="contactBtn">Contact</a></li>
  </ul>
</nav>

<!-- Hero -->
<section class="hero" id="home">
  <h1>Welcome to My Ultimate Website 💙</h1>
  <p>Interactive, fast, and most beautiful website!</p>
  <button onclick="showMessage(1)">Click Me</button>
  <button onclick="showMessage(2)">Second Button</button>
  <div class="particle" style="left:10%;animation-duration:8s;"></div>
  <div class="particle" style="left:30%;width:8px;height:8px;animation-duration:6s;"></div>
  <div class="particle" style="left:70%;width:12px;height:12px;animation-duration:10s;"></div>
</section>

<!-- Earnings -->
<section id="earnings">
  <h2>Earn with This Website 💰</h2>
  <p>Use affiliate links or donations to earn!</p>
  <button onclick="window.open('https://www.example.com/affiliate','_blank')">Affiliate Link</button>
  <button onclick="window.open('https://www.paypal.com/donate?hosted_button_id=XXXX','_blank')">Donate / Tip</button>
  <div style="margin-top:15px;"><h3>Live Clicks: <span id="clickCounter">0</span></h3></div>
</section>

<!-- About -->
<section id="about">
  <img src="https://picsum.photos/300?8" alt="About Image">
  <div>
    <h2>About Me</h2>
    <p>I am a web developer creating ultimate interactive websites with stunning visuals and animations.</p>
  </div>
</section>

<!-- Gallery -->
<section id="gallery">
  <h2>Gallery</h2>
  <img src="https://picsum.photos/200?5" alt="Image1">
  <img src="https://picsum.photos/200?6" alt="Image2">
  <img src="https://picsum.photos/200?7" alt="Image3">
</section>

<!-- Contact Modal -->
<div id="contactModal" class="modal">
  <div class="modal-content">
    <h2>Contact Me</h2>
    <input type="text" placeholder="Your Name">
    <input type="email" placeholder="Your Email">
    <textarea rows="4" placeholder="Your Message"></textarea><br>
    <button class="close-btn" id="closeModal">Close</button>
  </div>
</div>

<!-- Footer -->
<footer>
  © 2025 Ultimate Website | Designed with ❤ by You
  <div class="socials">
    <a href="#"><i class="fab fa-facebook"></i></a>
    <a href="#"><i class="fab fa-twitter"></i></a>
    <a href="#"><i class="fab fa-instagram"></i></a>
  </div>
</footer>

<button id="scrollTop"><i class="fas fa-arrow-up"></i></button>

<script>
// Hamburger menu
const hamburger=document.querySelector('.hamburger');
const navUl=document.querySelector('nav ul');
hamburger.addEventListener('click',()=>{navUl.classList.toggle('show');});

// Button click counter
let counter=0;
function showMessage(num){
  alert(num===1?'Great! You clicked the first button 😊':'Wow! You clicked the second button 😎');
  counter++;document.getElementById('clickCounter').innerText=counter;
}

// Scroll reveal
const sections=document.querySelectorAll('section');
window.addEventListener('scroll',()=>{
  const scrollPos=window.scrollY+window.innerHeight-100;
  sections.forEach(section=>{if(scrollPos>section.offsetTop){section.classList.add('visible');}});
});

// Contact modal
const modal=document.getElementById('contactModal');
const contactBtn=document.getElementById('contactBtn');
const closeBtn=document.getElementById('closeModal');
contactBtn.addEventListener('click',e=>{e.preventDefault();modal.style.display='flex';});
closeBtn.addEventListener('click',()=>{modal.style.display='none';});

// Scroll to top button
const scrollTopBtn=document.getElementById('scrollTop');
window.addEventListener('scroll',()=>{if(window.scrollY>300){scrollTopBtn.style.display='block';}else{scrollTopBtn.style.display='none';}});
scrollTopBtn.addEventListener('click',()=>{window.scrollTo({top:0,behavior:'smooth'});});
</script>

</body>
</html><!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Ultimate All-Level Website</title>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
<style>
/* ---------------- Basic Reset ---------------- */
*{margin:0;padding:0;box-sizing:border-box;}
body{
  font-family:'Poppins',sans-serif;
  scroll-behavior:smooth;
  color:#222;
  background:linear-gradient(-45deg,#ff9a9e,#fad0c4,#a18cd1,#fbc2eb);
  background-size:400% 400%;
  animation:gradientBG 12s ease infinite;
}
@keyframes gradientBG{0%{background-position:0% 50%;}50%{background-position:100% 50%;}100%{background-position:0% 50%;}}

/* ---------------- Navbar ---------------- */
nav{display:flex;justify-content:space-between;align-items:center;padding:15px 30px;position:sticky;top:0;background:rgba(0,0,0,0.3);z-index:100;}
nav .logo{font-size:1.5em;font-weight:bold;color:white;}
nav ul{list-style:none;display:flex;gap:20px;}
nav ul li a{text-decoration:none;color:white;font-weight:bold;padding:8px 12px;border-radius:8px;transition:0.3s;}
nav ul li a:hover, nav ul li a.active{background:#FFD700;color:#222;}
.hamburger{display:none;font-size:1.5em;color:white;cursor:pointer;}
@media(max-width:768px){nav ul{display:none;flex-direction:column;background:rgba(0,0,0,0.8);position:absolute;top:60px;right:0;width:200px;padding:20px;border-radius:10px;}nav ul.show{display:flex;}.hamburger{display:block;}}

/* ---------------- Hero ---------------- */
.hero{position:relative;text-align:center;padding:120px 20px;overflow:hidden;}
.hero h1{font-size:3em;background:linear-gradient(90deg,#ff6ec4,#7873f5,#42e695);-webkit-background-clip:text;-webkit-text-fill-color:transparent;animation:slideIn 1s forwards;margin-bottom:20px;}
.hero p{font-size:1.2em;margin-bottom:30px;animation:slideIn 1.2s forwards;}
.hero button{margin:5px;padding:12px 25px;font-size:1em;border:none;border-radius:8px;cursor:pointer;transition:0.3s;position:relative;overflow:hidden;}
.hero button::after{content:'';position:absolute;top:0;left:50%;width:0;height:100%;background:rgba(255,255,255,0.3);transition:0.5s;transform:translateX(-50%);border-radius:8px;}
.hero button:hover::after{width:100%;}
.hero button:hover{transform:scale(1.1);box-shadow:0 5px 15px rgba(0,0,0,0.3);}
@keyframes slideIn{0%{opacity:0;transform:translateY(100px);}100%{opacity:1;transform:translateY(0);}}

/* Floating Particles + Shapes */
.particle, .shape{position:absolute;border-radius:50%;opacity:0.7;animation:moveParticle linear infinite;}
.shape{border-radius:0;}

/* ---------------- Earnings ---------------- */
#earnings{margin:50px auto;max-width:700px;background:rgba(255,255,255,0.2);padding:30px;border-radius:20px;box-shadow:0 5px 15px rgba(0,0,0,0.2);text-align:center;transition:0.3s;}
#earnings button{margin:10px;padding:12px 25px;font-size:1em;border:none;border-radius:8px;cursor:pointer;background:#28a745;color:white;transition:0.3s;}
#earnings button:hover{background:#1e7e34;transform:scale(1.05);}

/* ---------------- About ---------------- */
#about{display:flex;flex-wrap:wrap;align-items:center;justify-content:center;gap:20px;padding:80px 20px;opacity:0;transform:translateY(50px);transition:all 0.8s ease-out;}
#about.visible{opacity:1;transform:translateY(0);}
#about img{width:300px;border-radius:15px;box-shadow:0 5px 15px rgba(0,0,0,0.2);}
#about div{text-align:left;max-width:500px;}

/* ---------------- Gallery ---------------- */
#gallery{padding:60px 20px;text-align:center;opacity:0;transform:translateY(50px);transition:all 0.8s ease-out;}
#gallery.visible{opacity:1;transform:translateY(0);}
#gallery img{width:200px;margin:10px;border-radius:15px;cursor:pointer;transition:transform 0.3s, box-shadow 0.3s;}
#gallery img:hover{transform:scale(1.1) rotate(2deg);box-shadow:0 8px 20px rgba(0,0,0,0.4);}

/* ---------------- Contact Modal ---------------- */
.modal{display:none;position:fixed;z-index:1000;left:0;top:0;width:100%;height:100%;background:rgba(0,0,0,0.7);justify-content:center;align-items:center;}
.modal-content{background:white;padding:20px;border-radius:15px;max-width:400px;width:90%;text-align:center;}
.modal input,.modal textarea{width:90%;padding:10px;margin:10px 0;border-radius:8px;border:1px solid #ccc;}
.close-btn{background:red;color:white;border:none;padding:10px 20px;cursor:pointer;border-radius:8px;}
.close-btn:hover{background:#cc0000;}

/* ---------------- Footer ---------------- */
footer{text-align:center;padding:20px;background:rgba(0,0,0,0.3);color:white;position:relative;}
.socials a{margin:0 10px;color:white;font-size:1.5em;transition:transform 0.3s,color 0.3s;}
.socials a:hover{color:#FFD700;transform:scale(1.3);}
#scrollTop{position:fixed;bottom:30px;right:30px;padding:10px 15px;background:#007bff;color:white;border:none;border-radius:50%;cursor:pointer;display:none;box-shadow:0 5px 10px rgba(0,0,0,0.3);}
#scrollTop:hover{background:#0056b3;}

/* Section Reveal */
section{opacity:0;transform:translateY(50px);transition:all 0.8s ease-out;}
section.visible{opacity:1;transform:translateY(0);}
</style>
</head>
<body>

<!-- Navbar -->
<nav>
  <div class="logo">UltimateSite</div>
  <div class="hamburger"><i class="fas fa-bars"></i></div>
  <ul>
    <li><a href="#home" class="active">Home</a></li>
    <li><a href="#about">About</a></li>
    <li><a href="#gallery">Gallery</a></li>
    <li><a href="#contact" id="contactBtn">Contact</a></li>
  </ul>
</nav>

<!-- Hero -->
<section class="hero" id="home">
  <h1>Welcome to Ultimate All-Level Website 💙</h1>
  <p>The most interactive, beautiful, and lively website ever!</p>
  <button onclick="showMessage(1)">Click Me</button>
  <button onclick="showMessage(2)">Second Button</button>
  <!-- Floating particles -->
  <div class="particle" style="left:10%;animation-duration:8s;width:10px;height:10px;"></div>
  <div class="particle" style="left:30%;width:8px;height:8px;animation-duration:6s;"></div>
  <div class="particle" style="left:70%;width:12px;height:12px;animation-duration:10s;"></div>
</section>

<!-- Earnings -->
<section id="earnings">
  <h2>Earn with This Website 💰</h2>
  <p>Use affiliate links or donations!</p>
  <button onclick="window.open('https://www.example.com/affiliate','_blank')">Affiliate Link</button>
  <button onclick="window.open('https://www.paypal.com/donate?hosted_button_id=XXXX','_blank')">Donate / Tip</button>
  <div style="margin-top:15px;"><h3>Live Clicks: <span id="clickCounter">0</span></h3></div>
</section>

<!-- About -->
<section id="about">
  <img src="https://picsum.photos/300?8" alt="About Image">
  <div>
    <h2>About Me</h2>
    <p>I am creating the ultimate, fully interactive, animated, and beautiful website with multiple levels of liveliness.</p>
  </div>
</section>

<!-- Gallery -->
<section id="gallery">
  <h2>Gallery</h2>
  <img src="https://picsum.photos/200?5" alt="Image1">
  <img src="https://picsum.photos/200?6" alt="Image2">
  <img src="https://picsum.photos/200?7" alt="Image3">
</section>

<!-- Contact Modal -->
<div id="contactModal" class="modal">
  <div class="modal-content">
    <h2>Contact Me</h2>
    <input type="text" placeholder="Your Name">
    <input type="email" placeholder="Your Email">
    <textarea rows="4" placeholder="Your Message"></textarea><br>
    <button class="close-btn" id="closeModal">Close</button>
  </div>
</div>

<!-- Footer -->
<footer>
  © 2025 Ultimate All-Level Website | Designed with ❤
  <div class="socials">
    <a href="#"><i class="fab fa-facebook"></i></a>
    <a href="#"><i class="fab fa-twitter"></i></a>
    <a href="#"><i class="fab fa-instagram"></i></a>
  </div>
</footer>

<button id="scrollTop"><i class="fas fa-arrow-up"></i></button>

<script>
// Hamburger menu
const hamburger=document.querySelector('.hamburger');
const navUl=document.querySelector('nav ul');
hamburger.addEventListener('click',()=>{navUl.classList.toggle('show');});

// Button click counter
let counter=0;
function showMessage(num){
  alert(num===1?'Great! You clicked the first button 😊':'Wow! You clicked the second button 😎');
  counter++;document.getElementById('clickCounter').innerText=counter;
}

// Scroll reveal
const sections=document.querySelectorAll('section');
window.addEventListener('scroll',()=>{
  const scrollPos=window.scrollY+window.innerHeight-100;
  sections.forEach(section=>{if(scrollPos>section.offsetTop){section.classList.add('visible');}});
});

// Contact modal
const modal=document.getElementById('contactModal');
const contactBtn=document.getElementById('contactBtn');
const closeBtn=document.getElementById('closeModal');
contactBtn.addEventListener('click',e=>{e.preventDefault();modal.style.display='flex';});
closeBtn.addEventListener('click',()=>{modal.style.display='none';});

// Scroll to top button
const scrollTopBtn=document.getElementById('scrollTop');
window.addEventListener('scroll',()=>{if(window.scrollY>300){scrollTopBtn.style.display='block';}else{scrollTopBtn.style.display='none';}});
scrollTopBtn.addEventListener('click',()=>{window.scrollTo({top:0,behavior:'smooth'});});
</script>

</body>
</html><!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Ultimate — All-in-One Interactive Website</title>

<!-- Google Font -->
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
<!-- Font Awesome -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

<style>
  /* ---------- Reset & Base ---------- */
  *{box-sizing:border-box;margin:0;padding:0}
  html,body{height:100%}
  body{
    font-family:'Poppins',sans-serif;
    color:#1c1c1c;
    background:linear-gradient(120deg,#ffd1dc,#cfe9ff,#e6f9d9);
    background-size:400% 400%;
    animation:bgShift 14s ease infinite;
    -webkit-font-smoothing:antialiased;
    -moz-osx-font-smoothing:grayscale;
    overflow-x:hidden;
  }
  @keyframes bgShift{
    0%{background-position:0% 50%}
    50%{background-position:100% 50%}
    100%{background-position:0% 50%}
  }
  a{color:inherit}
  button{font-family:inherit}

  /* ---------- Nav ---------- */
  nav{
    position:fixed;left:0;right:0;top:0;height:64px;
    display:flex;align-items:center;justify-content:space-between;
    padding:0 22px;z-index:1200;
    background:rgba(255,255,255,0.08);backdrop-filter:blur(6px);
    border-bottom:1px solid rgba(255,255,255,0.06);
  }
  .logo{font-weight:700;color:#fff;letter-spacing:0.2px}
  .nav-links{display:flex;gap:16px;align-items:center}
  .nav-links a{
    color:#fff;padding:8px 12px;border-radius:8px;text-decoration:none;font-weight:600;
    transition:all .22s;
  }
  .nav-links a:hover{background:rgba(255,255,255,0.12);color:#111}
  .hamburger{display:none;color:#fff;font-size:20px;cursor:pointer;padding:8px}

  @media(max-width:880px){
    .nav-links{position:fixed;right:18px;top:64px;background:rgba(0,0,0,0.7);flex-direction:column;padding:12px;border-radius:10px;display:none}
    .nav-links.show{display:flex}
    .hamburger{display:block}
  }

  /* ---------- Hero ---------- */
  header.hero{
    min-height:100vh;padding-top:84px;display:flex;align-items:center;justify-content:center;position:relative;
  }
  .hero-inner{max-width:1100px;padding:40px;text-align:center}
  .hero-title{font-size:2.6rem;line-height:1.05;margin-bottom:14px;font-weight:800;color:#fff}
  .hero-sub{font-size:1.05rem;color:rgba(255,255,255,0.92);margin-bottom:22px;opacity:0.95}
  .hero-cta{display:flex;gap:12px;justify-content:center;flex-wrap:wrap;margin-top:18px}
  .btn{
    background:linear-gradient(90deg,#6a5cff,#00c6ff);color:#fff;padding:12px 20px;border-radius:12px;border:none;font-weight:700;
    cursor:pointer;box-shadow:0 8px 30px rgba(80,60,200,0.14);position:relative;overflow:hidden;transition:transform .18s,box-shadow .18s;
  }
  .btn.secondary{background:linear-gradient(90deg,#ff9a9e,#fecfef);color:#111}
  .btn:hover{transform:translateY(-4px);box-shadow:0 18px 40px rgba(80,60,200,0.18)}
  .btn:active{transform:translateY(-1px)}

  /* ripple effect */
  .ripple { position:absolute; border-radius:50%; transform:scale(0); animation:ripple .6s linear; background:rgba(255,255,255,0.35); }
  @keyframes ripple { to{ transform:scale(4); opacity:0; } }

  /* typing */
  .typing{display:inline-block;color:#fff;font-weight:700}
  .typing-cursor{display:inline-block;width:3px;height:1.05em;background:#fff;margin-left:6px;animation:blink .8s steps(2) infinite}
  @keyframes blink{50%{opacity:0}}

  /* floating shapes */
  .floating-shape{position:absolute;opacity:0.12;filter:blur(.4px);pointer-events:none}
  /* particles canvas sits over hero */
  canvas#particles{position:absolute;left:0;top:0;right:0;bottom:0;z-index:2}

  /* ---------- Earnings / Monetization ---------- */
  .section{padding:64px 20px;max-width:1200px;margin:0 auto}
  .cards{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
  .card{
    background:linear-gradient(180deg,rgba(255,255,255,0.98),#ffffff);
    border-radius:14px;padding:18px;box-shadow:0 10px 30px rgba(20,20,40,0.06);
    transform-style:preserve-3d;transition:transform .28s,box-shadow .28s;
  }
  .card:hover{transform:translateY(-8px) rotateX(2deg) rotateY(2deg);box-shadow:0 30px 50px rgba(10,10,40,0.09)}
  .card h3{margin-bottom:8px;font-size:1.05rem}
  .card p{font-size:.95rem;color:#444;margin-bottom:12px}
  .card .small{font-size:.85rem;color:#666}
  .progress-wrap{height:10px;background:rgba(0,0,0,0.06);border-radius:999px;overflow:hidden;margin-top:10px}
  .progress-bar{height:100%;width:0;background:linear-gradient(90deg,#6a5cff,#00c6ff);transition:width 1.2s ease}

  @media(max-width:980px){.cards{grid-template-columns:repeat(2,1fr)}}
  @media(max-width:640px){.cards{grid-template-columns:1fr}}

  /* ---------- About ---------- */
  .about-grid{display:flex;gap:20px;align-items:center}
  .about-grid img{width:360px;border-radius:14px;box-shadow:0 12px 35px rgba(10,10,30,0.08)}
  .skills{margin-top:12px;display:flex;flex-direction:column;gap:10px}
  .skill{display:flex;justify-content:space-between;align-items:center;font-weight:600}
  .skill .bar{flex:1;margin-left:12px;height:8px;background:rgba(0,0,0,0.06);border-radius:999px;overflow:hidden}
  .skill .bar .fill{height:100%;width:0;background:linear-gradient(90deg,#42e695,#3bb0ff);transition:width 1.3s ease}

  @media(max-width:980px){.about-grid{flex-direction:column}.about-grid img{width:100%}}

  /* ---------- Gallery ---------- */
  .gallery-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:12px}
  .gallery-grid img{width:100%;display:block;border-radius:12px;cursor:pointer;transition:transform .3s,filter .3s;box-shadow:0 8px 30px rgba(10,10,20,0.06)}
  .gallery-grid img:hover{transform:scale(1.05) rotate(1deg);filter:saturate(1.15)}

  /* lightbox */
  .lightbox{position:fixed;inset:0;background:rgba(0,0,0,0.85);display:none;align-items:center;justify-content:center;z-index:1400}
  .lightbox img{max-width:92%;max-height:86%;border-radius:8px;box-shadow:0 30px 70px rgba(0,0,0,0.6)}
  .lightbox .close,.lightbox .next,.lightbox .prev{position:absolute;top:50%;transform:translateY(-50%);background:rgba(255,255,255,0.08);color:#fff;border:none;padding:12px;border-radius:50%;cursor:pointer}
  .lightbox .close{top:20px;right:20px;transform:none}
  .lightbox .prev{left:20px}
  .lightbox .next{right:20px}

  /* ---------- Contact Modal (floating labels) ---------- */
  .modal{position:fixed;inset:0;display:none;align-items:center;justify-content:center;background:rgba(0,0,0,0.6);z-index:1500}
  .modal-box{background:#fff;width:96%;max-width:480px;padding:22px;border-radius:14px;box-shadow:0 30px 80px rgba(0,0,0,0.15)}
  .field{position:relative;margin:14px 0}
  .field input,.field textarea{
    width:100%;padding:14px 14px;border-radius:10px;border:1px solid rgba(0,0,0,0.08);outline:none;font-size:1rem;
  }
  .field label{position:absolute;top:12px;left:14px;color:#747474;transition:all .18s;pointer-events:none}
  .field input:focus + label, .field input:not(:placeholder-shown) + label,
  .field textarea:focus + label, .field textarea:not(:placeholder-shown) + label{
    transform:translateY(-24px) scale(.92);color:#333
  }
  .modal .submit{background:linear-gradient(90deg,#6a5cff,#00c6ff);border:none;color:#fff;padding:12px 18px;border-radius:10px;font-weight:700;cursor:pointer}

  /* ---------- Footer ---------- */
  footer{padding:30px 20px;text-align:center;color:#fff;background:linear-gradient(90deg,rgba(0,0,0,0.05),rgba(255,255,255,0.02))}
  .socials{margin-top:12px;display:flex;gap:12px;justify-content:center}
  .socials a{display:inline-flex;align-items:center;justify-content:center;width:42px;height:42px;border-radius:10px;background:rgba(255,255,255,0.06);color:#fff;text-decoration:none;transition:transform .24s, background .24s}
  .socials a:hover{transform:translateY(-6px);background:linear-gradient(90deg,#ff9a9e,#fecfef)}

  /* scrollTop */
  #scrollTop{position:fixed;right:20px;bottom:22px;background:#6a5cff;color:#fff;border:none;padding:12px;border-radius:999px;display:none;z-index:1600;box-shadow:0 10px 30px rgba(80,60,200,0.18);cursor:pointer}

  /* small screens tweaks */
  @media(max-width:640px){
    .hero-title{font-size:1.6rem}
    .logo{font-size:1.1rem}
  }
</style>
</head>
<body>

<!-- Particles Canvas (for hero) -->
<canvas id="particles"></canvas>

<!-- Floating SVG shapes for layered depth -->
<svg class="floating-shape" width="120" height="120" style="left:-20px;top:80px;transform:rotate(12deg)">
  <circle cx="60" cy="60" r="60" fill="#fff"></circle>
</svg>
<svg class="floating-shape" width="160" height="100" style="right:-40px;bottom:160px;transform:rotate(40deg)">
  <rect width="160" height="100" fill="#fff"></rect>
</svg>

<!-- NAV -->
<nav>
  <div class="logo">UltimateSite</div>
  <div class="nav-links" id="navLinks">
    <a href="#home" class="nav-item">Home</a>
    <a href="#earn" class="nav-item">Earn</a>
    <a href="#about" class="nav-item">About</a>
    <a href="#gallery" class="nav-item">Gallery</a>
    <a href="#contact" id="contactBtn" class="nav-item">Contact</a>
    <button id="soundToggle" title="Toggle ambient sound" style="margin-left:6px;border-radius:10px;padding:8px 10px;border:none;background:rgba(255,255,255,0.06);color:#fff;cursor:pointer"><i class="fa fa-volume-up"></i></button>
  </div>
  <div class="hamburger" id="hamburgerBtn"><i class="fa fa-bars"></i></div>
</nav>

<!-- HERO -->
<header class="hero" id="home" aria-label="Hero">
  <div class="hero-inner">
    <h1 class="hero-title">
      <span id="typedLine">Welcome to the Ultimate Website</span>
      <span class="typing-cursor" id="typingCursor"></span>
    </h1>
    <p class="hero-sub">Interactive • Animated • Monetizable • Mobile-friendly</p>

    <div class="hero-cta">
      <button class="btn" id="primaryBtn">Get Started</button>
      <button class="btn secondary" id="donateBtn">Donate / Tip</button>
    </div>

    <div style="margin-top:18px;color:rgba(255,255,255,0.85)">
      <small>Try clicking buttons — confetti & animations included ✨</small>
    </div>
  </div>
</header>

<!-- EARNINGS -->
<section class="section" id="earn">
  <div class="cards">
    <div class="card" data-card="affiliate">
      <h3>Affiliate</h3>
      <p>Promote products using your affiliate link and earn commissions.</p>
      <div class="small">Clicks: <strong id="affClicks">0</strong></div>
      <div class="progress-wrap" style="margin-top:10px"><div class="progress-bar" id="affBar" style="width:0%"></div></div>
      <div style="margin-top:12px"><button class="btn" data-aff>Open Affiliate</button></div>
    </div>

    <div class="card" data-card="donate">
      <h3>Donate / Tip</h3>
      <p>Add a donate link (PayPal / UPI / Razorpay) to accept tips from visitors.</p>
      <div class="small">Donations: <strong id="donClicks">0</strong></div>
      <div class="progress-wrap" style="margin-top:10px"><div class="progress-bar" id="donBar" style="width:0%"></div></div>
      <div style="margin-top:12px"><button class="btn" id="donateOpen">Donate Now</button></div>
    </div>

    <div class="card" data-card="product">
      <h3>Product</h3>
      <p>Sell a small digital product or mini-course. Instant delivery link possible.</p>
      <div class="small">Sales: <strong id="prodClicks">0</strong></div>
      <div class="progress-wrap" style="margin-top:10px"><div class="progress-bar" id="prodBar" style="width:0%"></div></div>
      <div style="margin-top:12px"><button class="btn" id="buyOpen">Buy Product</button></div>
    </div>
  </div>
</section>

<!-- ABOUT -->
<section class="section" id="about">
  <div class="about-grid">
    <img src="https://picsum.photos/seed/about/520/360" alt="About image" loading="lazy">
    <div>
      <h2>About Me</h2>
      <p>I build beautiful, interactive websites with animations, monetization and responsive design. I focus on performance and delightful UX.</p>

      <div class="skills">
        <div class="skill">
          <span>HTML & CSS</span>
          <div class="bar" aria-hidden><div class="fill" data-percent="95"></div></div>
        </div>
        <div class="skill">
          <span>JavaScript</span>
          <div class="bar"><div class="fill" data-percent="90"></div></div>
        </div>
        <div class="skill">
          <span>Design & UI</span>
          <div class="bar"><div class="fill" data-percent="88"></div></div>
        </div>
        <div class="skill">
          <span>Monetization</span>
          <div class="bar"><div class="fill" data-percent="75"></div></div>
        </div>
      </div>

    </div>
  </div>
</section>

<!-- GALLERY -->
<section class="section" id="gallery">
  <h2 style="text-align:center;margin-bottom:18px">Gallery</h2>
  <div class="gallery-grid" id="galleryGrid">
    <img src="https://picsum.photos/seed/g1/600/400" alt="g1" loading="lazy">
    <img src="https://picsum.photos/seed/g2/500/700" alt="g2" loading="lazy">
    <img src="https://picsum.photos/seed/g3/700/500" alt="g3" loading="lazy">
    <img src="https://picsum.photos/seed/g4/600/600" alt="g4" loading="lazy">
    <img src="https://picsum.photos/seed/g5/800/540" alt="g5" loading="lazy">
    <img src="https://picsum.photos/seed/g6/540/540" alt="g6" loading="lazy">
  </div>
</section>

<!-- LIGHTBOX -->
<div class="lightbox" id="lightbox">
  <button class="close" id="lbClose"><i class="fa fa-times"></i></button>
  <button class="prev" id="lbPrev"><i class="fa fa-chevron-left"></i></button>
  <img id="lbImg" src="" alt="lightbox image">
  <button class="next" id="lbNext"><i class="fa fa-chevron-right"></i></button>
</div>

<!-- CONTACT -->
<section class="section" id="contact" style="text-align:center">
  <h2 style="margin-bottom:12px">Contact</h2>
  <p style="margin-bottom:18px">Send a message — form below opens in a modal</p>
  <button class="btn" id="openContact">Open Contact Form</button>
</section>

<!-- CONTACT MODAL -->
<div class="modal" id="contactModal">
  <div class="modal-box">
    <h3 style="margin-bottom:8px">Send a Message</h3>
    <div class="field">
      <input id="cname" type="text" placeholder=" " required>
      <label for="cname">Your Name</label>
    </div>
    <div class="field">
      <input id="cemail" type="email" placeholder=" " required>
      <label for="cemail">Your Email</label>
    </div>
    <div class="field">
      <textarea id="cmsg" rows="4" placeholder=" " required></textarea>
      <label for="cmsg">Your Message</label>
    </div>
    <div style="display:flex;gap:10px;justify-content:center;margin-top:10px">
      <button class="submit" id="sendMsg">Send</button>
      <button class="close-btn" id="closeContact">Close</button>
    </div>
  </div>
</div>

<!-- FOOTER -->
<footer>
  <div>© <span id="year"></span> UltimateSite • Built with ❤</div>
  <div class="socials">
    <a href="#" aria-label="facebook"><i class="fab fa-facebook-f"></i></a>
    <a href="#" aria-label="twitter"><i class="fab fa-twitter"></i></a>
    <a href="#" aria-label="instagram"><i class="fab fa-instagram"></i></a>
  </div>
</footer>

<button id="scrollTop" title="Back to top"><i class="fa fa-arrow-up"></i></button>

<!-- Ambient audio (optional) -->
<audio id="ambientAudio" loop src=""></audio>

<!-- Confetti canvas -->
<canvas id="confetti" style="position:fixed;left:0;top:0;right:0;bottom:0;pointer-events:none;z-index:2000"></canvas>

<script>
/* ================== Utilities ================== */

// small helper
const $ = (s,root=document) => root.querySelector(s);
const $$ = (s,root=document) => Array.from(root.querySelectorAll(s));

/* ============== NAV & HAMBURGER ============== */
const hamburger = $('#hamburgerBtn');
const navLinks = $('#navLinks');
hamburger && hamburger.addEventListener('click', ()=> navLinks.classList.toggle('show'));

// smooth highlight nav on click
$$('.nav-item').forEach(a=>{
  a.addEventListener('click', ()=>{
    $$('.nav-item').forEach(n=>n.classList.remove('active'));
    a.classList.add('active');
    if(navLinks.classList.contains('show')) navLinks.classList.remove('show');
  });
});

/* ============== TYPING (multi-line loop) ============== */
const typingTexts = ["Welcome to the Ultimate Website","Interactive • Fast • Stunning","Click buttons & feel the magic ✨"];
let typingIndex = 0, charIndex = 0, forward = true;
const typedLine = $('#typedLine');
const cursor = $('#typingCursor');

function typeLoop(){
  const txt = typingTexts[typingIndex];
  if(forward){
    charIndex++;
    typedLine.textContent = txt.slice(0,charIndex);
    if(charIndex===txt.length){ forward=false; setTimeout(typeLoop,900); return; }
  }else{
    charIndex--;
    typedLine.textContent = txt.slice(0,charIndex);
    if(charIndex===0){ forward=true; typingIndex=(typingIndex+1)%typingTexts.length; setTimeout(typeLoop,200); return; }
  }
  setTimeout(typeLoop, forward?60:24);
}
typeLoop();

/* ============== PARTICLES (canvas in hero) ============== */
const pCanvas = $('#particles');
const confettiCanvas = $('#confetti');
const dpr = Math.max(1,window.devicePixelRatio || 1);
function fitCanvas(canvas){
  canvas.width = Math.floor(window.innerWidth * dpr);
  canvas.height = Math.floor(window.innerHeight * dpr);
  canvas.style.width = window.innerWidth + 'px';
  canvas.style.height = window.innerHeight + 'px';
}
fitCanvas(pCanvas); fitCanvas(confettiCanvas);

let particles = [];
function initParticles(){
  particles = [];
  const num = Math.floor(window.innerWidth / 40);
  for(let i=0;i<num;i++){
    particles.push({
      x: Math.random()*window.innerWidth,
      y: window.innerHeight + Math.random()*800,
      size: 2+Math.random()*8,
      speed: 0.3 + Math.random()*0.9,
      hue: 180 + Math.random()*120,
      alpha: 0.08 + Math.random()*0.2
    });
  }
}
initParticles();

const pc = pCanvas.getContext('2d');
function drawParticles(){
  pc.clearRect(0,0,pCanvas.width,pCanvas.height);
  for(let p of particles){
    pc.beginPath();
    pc.fillStyle = hsla(${p.hue},80%,60%,${p.alpha});
    pc.arc(p.x * dpr, (p.y) * dpr, p.size * dpr, 0, Math.PI*2);
    pc.fill();
    p.y -= p.speed;
    p.x += Math.sin(Date.now()/500 + p.size) * 0.3;
    if(p.y < -50){ p.y = window.innerHeight + Math.random()*200; p.x = Math.random()*window.innerWidth; }
  }
  requestAnimationFrame(drawParticles);
}
drawParticles();
window.addEventListener('resize', ()=> { fitCanvas(pCanvas); fitCanvas(confettiCanvas); initParticles(); });

/* ============== CONFETTI (on button clicks) ============== */
const confCtx = confettiCanvas.getContext('2d');
let confettiPieces = [];
function spawnConfetti(x, y, count=36){
  const rect = confettiCanvas.getBoundingClientRect();
  for(let i=0;i<count;i++){
    confettiPieces.push({
      x: (x||rect.width/2) * dpr,
      y: (y||rect.height/2) * dpr,
      vx: (Math.random()*8 - 4) * dpr,
      vy: (Math.random()*-10 - 2) * dpr,
      size: (4 + Math.random()*8) * dpr,
      rot: Math.random()*360,
      color: hsl(${Math.random()*360},70%,60%),
      life: 80 + Math.random()*60
    });
  }
}
function drawConfetti(){
  confCtx.clearRect(0,0,confettiCanvas.width,confettiCanvas.height);
  for(let i=confettiPieces.length-1;i>=0;i--){
    const p = confettiPieces[i];
    confCtx.save();
    confCtx.translate(p.x, p.y);
    confCtx.rotate(p.rot*Math.PI/180);
    confCtx.fillStyle = p.color;
    confCtx.fillRect(-p.size/2, -p.size/2, p.size, p.size*0.6);
    confCtx.restore();
    p.x += p.vx; p.y += p.vy;
    p.vy += 0.25 * dpr; p.rot += p.vx * 0.5;
    p.life--;
    if(p.life<=0 || p.y > window.innerHeight * dpr + 50) confettiPieces.splice(i,1);
  }
  requestAnimationFrame(drawConfetti);
}
drawConfetti();

/* attach confetti to buttons + ripple effect */
function bindButtonEffects(btn){
  btn.addEventListener('click', (e)=>{
    // ripple
    const r = document.createElement('span');
    const rect = btn.getBoundingClientRect();
    r.className = 'ripple';
    r.style.left = (e.clientX - rect.left) + 'px';<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Badal Store — Cart Demo</title>
  <style>
    :root {
      --accent: #f06595;
      --accent-2: #ff6b6b;
      --bg: #f3f6fa;
      --card: #ffffff;
      --muted: #6b7280;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: "Poppins", sans-serif;
      background:var(--bg);
      color:#111827;
    }

    /* header */
    header{
      display:flex;
      justify-content:space-between;
      align-items:center;
      padding:16px 28px;
      background: linear-gradient(135deg,var(--accent),var(--accent-2));
      color:white;
      position:sticky;
      top:0;
      z-index:20;
    }
    header h1{font-size:20px; letter-spacing:1px}
    .nav {display:flex; gap:18px; align-items:center}
    .cart-btn{
      background:rgba(255,255,255,0.16);
      padding:8px 12px;
      border-radius:999px;
      cursor:pointer;
      display:flex;
      gap:8px;
      align-items:center;
      position:relative;
    }
    .cart-count{
      position:absolute;
      top:-6px; right:-6px;
      background:#0b1220; color:white;
      width:20px; height:20px; border-radius:50%;
      display:flex; align-items:center; justify-content:center;
      font-size:12px; font-weight:600;
    }

    /* hero */
    .hero{
      text-align:center;
      padding:40px 20px;
      color:white;
      background: linear-gradient(135deg,#ffd6e0,#ffb3c6);
      margin-bottom:18px;
    }
    .hero h2{margin:0 0 8px;font-size:26px;color:#111827}
    .hero p{margin:0;color:var(--muted)}

    /* products grid */
    .products{
      padding:20px;
      display:grid;
      grid-template-columns: repeat(auto-fit, minmax(220px,1fr));
      gap:18px;
      max-width:1200px;
      margin:0 auto;
    }
    .product{
      background:var(--card);
      border-radius:12px;
      box-shadow:0 6px 18px rgba(13,18,26,0.06);
      overflow:hidden;
      display:flex;
      flex-direction:column;
      transition:transform .18s, box-shadow .18s;
    }
    .product:hover{ transform:translateY(-6px); box-shadow:0 14px 30px rgba(13,18,26,0.08) }
    .product img{ width:100%; height:170px; object-fit:cover; display:block }
    .product-body{ padding:12px; text-align:center; flex:1; display:flex; flex-direction:column; justify-content:space-between }
    .product h3{ font-size:16px; margin:8px 0 4px }
    .price{ color:var(--accent-2); font-weight:700; margin-bottom:10px }
    .actions{ display:flex; gap:8px; justify-content:center; padding-bottom:12px }
    .btn{
      border:0; outline:0;
      padding:8px 14px; border-radius:999px;
      cursor:pointer; background:var(--accent);
      color:white; font-weight:600;
    }
    .btn.secondary{ background:#eef2ff; color:#111827; box-shadow:inset 0 -3px 0 rgba(0,0,0,0.04) }

    /* cart sidebar */
    .cart-panel{
      position:fixed;
      right:0; top:0; height:100vh;
      width:360px; max-width:92vw;
      background:#fff; box-shadow: -20px 0 40px rgba(2,6,23,0.12);
      transform:translateX(110%);
      transition:transform .28s;
      z-index:60;
      display:flex; flex-direction:column;
    }
    .cart-panel.open{ transform:translateX(0) }

    .cart-head{
      padding:18px; border-bottom:1px solid #f3f4f6; display:flex; justify-content:space-between; align-items:center;
    }
    .cart-items{ padding:16px; overflow:auto; flex:1 }
    .cart-item{
      display:flex; gap:12px; margin-bottom:14px; align-items:center;
      border-radius:10px; padding:8px;
      background:#fafafa;
    }
    .cart-item img{ width:64px; height:64px; object-fit:cover; border-radius:8px }
    .ci-body{ flex:1 }
    .ci-body h4{ margin:0; font-size:14px }
    .ci-body p{ margin:6px 0 0; color:var(--muted); font-size:13px }
    .qty-controls{ display:flex; gap:6px; margin-top:8px; align-items:center }
    .qty-controls button{ padding:6px 8px; border-radius:6px; border:0; cursor:pointer; background:#111827; color:white }

    .cart-foot{ padding:16px; border-top:1px solid #f3f4f6 }
    .total-row{ display:flex; justify-content:space-between; margin-bottom:12px; font-weight:700; font-size:16px }
    .checkout{ width:100%; padding:12px; border-radius:10px; border:0; background:linear-gradient(90deg,var(--accent),var(--accent-2)); color:white; font-weight:700; cursor:pointer }

    /* empty state */
    .empty{
      padding:40px; text-align:center; color:var(--muted);
    }

    /* responsive */
    @media (max-width:560px){
      header { padding:12px 14px }
      .hero h2{ font-size:20px }
    }
  </style>
</head>
<body>

  <header>
    <h1>🛍 Badal Store</h1>
    <div class="nav">
      <button class="cart-btn" id="openCart" aria-label="Open cart">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" style="filter:drop-shadow(0 1px 0 rgba(0,0,0,0.08))">
          <path d="M3 3h2l.4 2M7 13h10l3-8H6.4" stroke="white" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
          <circle cx="10" cy="20" r="1.6" fill="white"/><circle cx="18" cy="20" r="1.6" fill="white"/>
        </svg>
        <span style="opacity:.95">Cart</span>
        <div class="cart-count" id="cartCount">0</div>
      </button>
    </div>
  </header>

  <section class="hero">
    <h2>Trending Picks — Add to cart and try</h2>
    <p>Click "Add to Cart" — total and quantities update below in the cart panel.</p>
  </section>

  <main class="products" id="products">
    <!-- Products will be injected by JS -->
  </main>

  <!-- cart panel -->
  <aside class="cart-panel" id="cartPanel" aria-hidden="true">
    <div class="cart-head">
      <strong>Your Cart</strong>
      <div style="display:flex; gap:8px; align-items:center">
        <button id="clearCart" class="btn secondary" title="Empty cart">Clear</button>
        <button id="closeCart" class="btn secondary">Close</button>
      </div>
    </div>
    <div class="cart-items" id="cartItems">
      <!-- items injected -->
    </div>

    <div class="cart-foot">
      <div class="total-row">
        <div>Subtotal</div>
        <div id="cartTotal">₹0</div>
      </div>
      <button class="checkout" id="checkoutBtn">Checkout</button>
    </div>
  </aside>

  <script>
    /***********************
     * Sample products list *
     ***********************/
    const PRODUCTS = [
      { id: 'p1', title: 'Stylish Sneakers', price: 1499, img: 'https://images.unsplash.com/photo-1606813908821-01c3b9435b9d?auto=format&fit=crop&w=800&q=60' },
      { id: 'p2', title: 'Smart Watch', price: 2999, img: 'https://images.unsplash.com/photo-1526170375885-4d8ecf77b99f?auto=format&fit=crop&w=800&q=60' },
      { id: 'p3', title: 'Wireless Headphones', price: 1899, img: 'https://images.unsplash.com/photo-1600180758890-6c6098a99c91?auto=format&fit=crop&w=800&q=60' },
      { id: 'p4', title: 'Leather Bag', price: 1299, img: 'https://images.unsplash.com/photo-1585386959984-a4155223f7d2?auto=format&fit=crop&w=800&q=60' }
    ];

    /* ---------- helpers ---------- */
    const qs = sel => document.querySelector(sel);
    const qsa = sel => document.querySelectorAll(sel);
    const formatINR = n => '₹' + n.toLocaleString('en-IN');

    /* ---------- render products ---------- */
    const productsEl = qs('#products');
    function renderProducts(){
      productsEl.innerHTML = '';
      for(const p of PRODUCTS){
        const card = document.createElement('article');
        card.className = 'product';
        card.innerHTML = `
          <img src="${p.img}" alt="${p.title}" loading="lazy">
          <div class="product-body">
            <div>
              <h3>${p.title}</h3>
              <div class="price">${formatINR(p.price)}</div>
            </div>
            <div class="actions">
              <button class="btn add-to-cart" data-id="${p.id}">Add to Cart</button>
              <button class="btn secondary view" data-id="${p.id}">View</button>
            </div>
          </div>
        `;
        productsEl.appendChild(card);
      }
    }

    /* ---------- Cart logic (with localStorage) ---------- */
    const STORAGE_KEY = 'badal_store_cart_v1';
    let cart = {}; // { productId: {id, title, price, qty, img} }

    function loadCart(){
      try {
        const raw = localStorage.getItem(STORAGE_KEY);
        cart = raw ? JSON.parse(raw) : {};
      } catch(e){
        cart = {};
      }
    }
    function saveCart(){
      localStorage.setItem(STORAGE_KEY, JSON.stringify(cart));
    }

    function addToCart(productId, qty = 1){
      const p = PRODUCTS.find(x => x.id === productId);
      if(!p) return;
      if(cart[p.id]){
        cart[p.id].qty += qty;
      } else {
        cart[p.id] = { id: p.id, title: p.title, price: p.price, qty: qty, img: p.img };
      }
      saveCart();
      updateCartUI();
      openCartPanel();
    }

    function removeFromCart(productId){
      delete cart[productId];
      saveCart();
      updateCartUI();
    }

    function changeQty(productId, newQty){
      if(!cart[productId]) return;
      cart[productId].qty = Math.max(0, Math.floor(newQty));
      if(cart[productId].qty === 0) delete cart[productId];
      saveCart();
      updateCartUI();
    }

    function clearCart(){
      cart = {};
      saveCart();
      updateCartUI();
    }

    function cartTotals(){
      let count = 0, total = 0;
      for(const k in cart){
        count += cart[k].qty;
        total += cart[k].qty * cart[k].price;
      }
      return { count, total };
    }

    /* ---------- UI updates ---------- */
    const cartCountEl = qs('#cartCount');
    const cartItemsEl = qs('#cartItems');
    const cartTotalEl = qs('#cartTotal');
    const cartPanel = qs('#cartPanel');

    function updateCartUI(){
      const { count, total } = cartTotals();
      cartCountEl.textContent = count;
      cartTotalEl.textContent = formatINR(total);

      // render items
      cartItemsEl.innerHTML = '';
      if(count === 0){
        cartItemsEl.innerHTML = <div class="empty"><strong>Your cart is empty</strong><div style="margin-top:8px;color:var(--muted)">Add items to see them here.</div></div>;
        return;
      }

      for(const k in cart){
        const it = cart[k];
        const row = document.createElement('div');
        row.className = 'cart-item';
        row.innerHTML = `
          <img src="${it.img}" alt="${it.title}">
          <div class="ci-body">
            <h4>${it.title}</h4>
            <p>${formatINR(it.price)} • ${formatINR(it.price * it.qty)}</p>
            <div class="qty-controls">
              <button class="qty-minus" data-id="${it.id}">-</button>
              <div style="min-width:36px; text-align:center;">Qty: <strong>${it.qty}</strong></div>
              <button class="qty-plus" data-id="${it.id}">+</button>
              <button class="btn secondary" style="margin-left:8px" data-remove="${it.id}">Remove</button>
            </div>
          </div>
        `;
        cartItemsEl.appendChild(row);
      }

      // attach listeners for plus/minus/remove
      qsa('.qty-plus').forEach(btn => btn.addEventListener('click', e=>{
        const id = e.currentTarget.dataset.id; changeQty(id, (cart[id].qty + 1));
      }));
      qsa('.qty-minus').forEach(btn => btn.addEventListener('click', e=>{
        const id = e.currentTarget.dataset.id; changeQty(id, (cart[id].qty - 1));
      }));
      qsa('[data-remove]').forEach(btn => btn.addEventListener('click', e=>{
        const id = e.currentTarget.getAttribute('data-remove'); removeFromCart(id);
      }));
    }

    /* ---------- panel open/close ---------- */
    function openCartPanel(){
      cartPanel.classList.add('open');
      cartPanel.setAttribute('aria-hidden','false');
    }
    function closeCartPanel(){
      cartPanel.classList.remove('open');
      cartPanel.setAttribute('aria-hidden','true');
    }

    /* ---------- event bindings ---------- */
    function setupEvents(){
      // add-to-cart buttons
      document.body.addEventListener('click', e=>{
        const atc = e.target.closest('.add-to-cart');
        if(atc){
          const id = atc.dataset.id;
          addToCart(id, 1);
        }
      });

      // view button (simple alert / can be link to product page)
      document.body.addEventListener('click', e=>{
        const view = e.target.closest('.view');
        if(view){
          const id = view.dataset.id;
          const p = PRODUCTS.find(x=>x.id===id);
          if(p) alert(p.title + "\\nPrice: " + formatINR(p.price));
        }
      });

      // open/close cart
      qs('#openCart').addEventListener('click', ()=> openCartPanel());
      qs('#closeCart').addEventListener('click', ()=> closeCartPanel());

      // clear cart
      qs('#clearCart').addEventListener('click', ()=> {
        if(confirm('Empty the cart?')) clearCart();
      });

      // checkout
      qs('#checkoutBtn').addEventListener('click', ()=>{
        const { count, total } = cartTotals();
        if(count === 0){ alert('Cart is empty. Add items first.'); return; }
        // Simple checkout demo:
        alert('Checkout: ' + count + ' items — Total ' + formatINR(total) + '\\n(This is a demo — integrate payment to complete.)');
        // After successful checkout you might clear the cart:
        // clearCart();
      });

      // close cart when clicking outside (optional)
      window.addEventListener('keydown', (e)=>{
        if(e.key === 'Escape') closeCartPanel();
      });
    }

    /* ---------- init ---------- */
    function init(){
      renderProducts();
      loadCart();
      setupEvents();
      updateCartUI();
    }

    init();
  </script>
</body>
</html><!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Badal Store — Product Detail + Cart</title>
  <style>
    :root{
      --accent:#f06595; --accent-2:#ff6b6b; --bg:#f3f6fa; --card:#fff; --muted:#6b7280;
    }
    *{box-sizing:border-box}
    body{margin:0;font-family:"Poppins",sans-serif;background:var(--bg);color:#111827}

    /* header */
    header{display:flex;justify-content:space-between;align-items:center;padding:16px 28px;
      background:linear-gradient(135deg,var(--accent),var(--accent-2));color:white;position:sticky;top:0;z-index:20}
    header h1{font-size:20px;letter-spacing:1px}
    .nav{display:flex;gap:18px;align-items:center}
    .cart-btn{background:rgba(255,255,255,0.16);padding:8px 12px;border-radius:999px;cursor:pointer;display:flex;gap:8px;align-items:center;position:relative}
    .cart-count{position:absolute;top:-6px;right:-6px;background:#0b1220;color:white;width:20px;height:20px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:600}

    /* hero */
    .hero{text-align:center;padding:40px 20px;color:white;background:linear-gradient(135deg,#ffd6e0,#ffb3c6);margin-bottom:18px}
    .hero h2{margin:0 0 8px;font-size:26px;color:#111827}
    .hero p{margin:0;color:var(--muted)}

    /* products */
    .products{padding:20px;display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:18px;max-width:1200px;margin:0 auto}
    .product{background:var(--card);border-radius:12px;box-shadow:0 6px 18px rgba(13,18,26,0.06);overflow:hidden;display:flex;flex-direction:column;transition:transform .18s,box-shadow .18s}
    .product:hover{transform:translateY(-6px);box-shadow:0 14px 30px rgba(13,18,26,0.08)}
    .product img{width:100%;height:170px;object-fit:cover;display:block}
    .product-body{padding:12px;text-align:center;flex:1;display:flex;flex-direction:column;justify-content:space-between}
    .product h3{font-size:16px;margin:8px 0 4px}
    .price{color:var(--accent-2);font-weight:700;margin-bottom:10px}
    .actions{display:flex;gap:8px;justify-content:center;padding-bottom:12px}
    .btn{border:0;outline:0;padding:8px 14px;border-radius:999px;cursor:pointer;background:var(--accent);color:white;font-weight:600}
    .btn.secondary{background:#eef2ff;color:#111827;box-shadow:inset 0 -3px 0 rgba(0,0,0,0.04)}

    /* cart panel */
    .cart-panel{position:fixed;right:0;top:0;height:100vh;width:360px;max-width:92vw;background:#fff;box-shadow:-20px 0 40px rgba(2,6,23,0.12);transform:translateX(110%);transition:transform .28s;z-index:60;display:flex;flex-direction:column}
    .cart-panel.open{transform:translateX(0)}
    .cart-head{padding:18px;border-bottom:1px solid #f3f4f6;display:flex;justify-content:space-between;align-items:center}
    .cart-items{padding:16px;overflow:auto;flex:1}
    .cart-item{display:flex;gap:12px;margin-bottom:14px;align-items:center;border-radius:10px;padding:8px;background:#fafafa}
    .cart-item img{width:64px;height:64px;object-fit:cover;border-radius:8px}
    .ci-body{flex:1}
    .ci-body h4{margin:0;font-size:14px}
    .ci-body p{margin:6px 0 0;color:var(--muted);font-size:13px}
    .qty-controls{display:flex;gap:6px;margin-top:8px;align-items:center}
    .qty-controls button{padding:6px 8px;border-radius:6px;border:0;cursor:pointer;background:#111827;color:white}
    .cart-foot{padding:16px;border-top:1px solid #f3f4f6}
    .total-row{display:flex;justify-content:space-between;margin-bottom:12px;font-weight:700;font-size:16px}
    .checkout{width:100%;padding:12px;border-radius:10px;border:0;background:linear-gradient(90deg,var(--accent),var(--accent-2));color:white;font-weight:700;cursor:pointer}

    /* modal */
    .modal-backdrop{position:fixed;inset:0;background:rgba(2,6,23,0.45);display:none;align-items:center;justify-content:center;z-index:80;padding:16px}
    .modal-backdrop.open{display:flex}
    .modal{background:white;border-radius:12px;max-width:920px;width:100%;display:flex;gap:18px;overflow:hidden}
    .modal-left{flex:1;min-width:260px}
    .modal-left img{width:100%;height:100%;object-fit:cover;display:block}
    .modal-right{width:420px;padding:18px;display:flex;flex-direction:column}
    .modal-right h3{margin:0 0 8px}
    .modal-right p.desc{color:var(--muted);margin:0 0 12px}
    .modal-right .price{font-size:20px;margin-bottom:12px}
    .modal-actions{display:flex;gap:10px;margin-top:auto;align-items:center}
    .qty-select{display:flex;gap:8px;align-items:center}
    .qty-select button{padding:6px 10px;border-radius:8px;border:0;background:#111827;color:#fff;cursor:pointer}

    /* responsive */
    @media (max-width:820px){
      .modal{flex-direction:column}
      .modal-right{width:100%}
    }
    @media (max-width:560px){header{padding:12px 14px}.hero h2{font-size:20px}}
  </style>
</head>
<body>

  <header>
    <h1>🛍 Badal Store</h1>
    <div class="nav">
      <button class="cart-btn" id="openCart" aria-label="Open cart">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" style="filter:drop-shadow(0 1px 0 rgba(0,0,0,0.08))">
          <path d="M3 3h2l.4 2M7 13h10l3-8H6.4" stroke="white" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
          <circle cx="10" cy="20" r="1.6" fill="white"/><circle cx="18" cy="20" r="1.6" fill="white"/>
        </svg>
        <span style="opacity:.95">Cart</span>
        <div class="cart-count" id="cartCount">0</div>
      </button>
    </div>
  </header>

  <section class="hero">
    <h2>Trending Picks — Click View for details</h2>
    <p>Modal product detail with Add to Cart & Buy Now.</p>
  </section>

  <main class="products" id="products"></main>

  <!-- cart panel -->
  <aside class="cart-panel" id="cartPanel" aria-hidden="true">
    <div class="cart-head">
      <strong>Your Cart</strong>
      <div style="display:flex;gap:8px;align-items:center">
        <button id="clearCart" class="btn secondary" title="Empty cart">Clear</button>
        <button id="closeCart" class="btn secondary">Close</button>
      </div>
    </div>
    <div class="cart-items" id="cartItems"></div>
    <div class="cart-foot">
      <div class="total-row"><div>Subtotal</div><div id="cartTotal">₹0</div></div>
      <button class="checkout" id="checkoutBtn">Checkout</button>
    </div>
  </aside>

  <!-- product modal -->
  <div class="modal-backdrop" id="modalBackdrop" aria-hidden="true">
    <div class="modal" role="dialog" aria-modal="true" aria-labelledby="modalTitle">
      <div class="modal-left" id="modalImageWrap">
        <!-- image injected -->
      </div>
      <div class="modal-right">
        <h3 id="modalTitle">Product Title</h3>
        <div class="price" id="modalPrice">₹0</div>
        <p class="desc" id="modalDesc">Product description goes here.</p>

        <div style="display:flex;gap:12px;align-items:center;margin-top:6px">
          <div class="qty-select" id="modalQtySelect">
            <button id="modalQtyMinus">-</button>
            <div id="modalQty" style="min-width:36px;text-align:center;font-weight:700">1</div>
            <button id="modalQtyPlus">+</button>
          </div>
          <div style="margin-left:auto;color:var(--muted)">In Stock</div>
        </div>

        <div class="modal-actions">
          <button class="btn" id="modalAddToCart">Add to Cart</button>
          <button class="btn secondary" id="modalBuyNow">Buy Now</button>
          <button style="margin-left:auto;border:0;background:transparent;cursor:pointer;font-weight:600" id="modalClose">Close</button>
        </div>
      </div>
    </div>
  </div>

  <script>
    /***********************
     * Sample products list *
     ***********************/
    const PRODUCTS = [
      { id:'p1', title:'Stylish Sneakers', price:1499, img:'https://images.unsplash.com/photo-1606813908821-01c3b9435b9d?auto=format&fit=crop&w=1200&q=60', desc:'Comfortable, lightweight sneakers perfect for everyday wear.' },
      { id:'p2', title:'Smart Watch', price:2999, img:'https://images.unsplash.com/photo-1526170375885-4d8ecf77b99f?auto=format&fit=crop&w=1200&q=60', desc:'Track your fitness and notifications with this sleek smartwatch.' },
      { id:'p3', title:'Wireless Headphones', price:1899, img:'https://images.unsplash.com/photo-1600180758890-6c6098a99c91?auto=format&fit=crop&w=1200&q=60', desc:'Noise-cancelling wireless headphones with long battery life.' },
      { id:'p4', title:'Leather Bag', price:1299, img:'https://images.unsplash.com/photo-1585386959984-a4155223f7d2?auto=format&fit=crop&w=1200&q=60', desc:'Premium leather bag for work and travel.' }
    ];

    /* helpers */
    const qs = s => document.querySelector(s);
    const qsa = s => document.querySelectorAll(s);
    const formatINR = n => '₹' + n.toLocaleString('en-IN');

    /* render products */
    const productsEl = qs('#products');
    function renderProducts(){
      productsEl.innerHTML = '';
      for(const p of PRODUCTS){
        const card = document.createElement('article');
        card.className = 'product';
        card.innerHTML = `
          <img src="${p.img}" alt="${p.title}" loading="lazy">
          <div class="product-body">
            <div>
              <h3>${p.title}</h3>
              <div class="price">${formatINR(p.price)}</div>
            </div>
            <div class="actions">
              <button class="btn add-to-cart" data-id="${p.id}">Add to Cart</button>
              <button class="btn secondary view" data-id="${p.id}">View</button>
            </div>
          </div>
        `;
        productsEl.appendChild(card);
      }
    }

    /* Cart logic */
    const STORAGE_KEY = 'badal_store_cart_v2';
    let cart = {}; // {id:{id,title,price,qty,img}}
    function loadCart(){ try{ const raw=localStorage.getItem(STORAGE_KEY); cart = raw?JSON.parse(raw):{} }catch{cart={}} }
    function saveCart(){ localStorage.setItem(STORAGE_KEY, JSON.stringify(cart)) }
    function addToCart(productId, qty=1){
      const p = PRODUCTS.find(x=>x.id===productId); if(!p) return;
      if(cart[p.id]) cart[p.id].qty += qty; else cart[p.id] = { id:p.id, title:p.title, price:p.price, qty:qty, img:p.img };
      saveCart(); updateCartUI();
    }
    function changeQty(productId,newQty){
      if(!cart[productId]) return;
      cart[productId].qty = Math.max(0, Math.floor(newQty));
      if(cart[productId].qty === 0) delete cart[productId];
      saveCart(); updateCartUI();
    }
    function removeFromCart(productId){ delete cart[productId]; saveCart(); updateCartUI(); }
    function clearCart(){ cart = {}; saveCart(); updateCartUI(); }
    function cartTotals(){ let count=0,total=0; for(const k in cart){ count+=cart[k].qty; total+=cart[k].qty*cart[k].price } return {count,total} }

    /* UI elements */
    const cartCountEl = qs('#cartCount');
    const cartItemsEl = qs('#cartItems');
    const cartTotalEl = qs('#cartTotal');
    const cartPanel = qs('#cartPanel');

    function updateCartUI(){
      const {count,total} = cartTotals();
      cartCountEl.textContent = count;
      cartTotalEl.textContent = formatINR(total);

      cartItemsEl.innerHTML = '';
      if(count === 0){
        cartItemsEl.innerHTML = <div class="empty"><strong>Your cart is empty</strong><div style="margin-top:8px;color:var(--muted)">Add items to see them here.</div></div>;
        return;
      }

      for(const k in cart){
        const it = cart[k];
        const row = document.createElement('div');
        row.className = 'cart-item';
        row.innerHTML = `
          <img src="${it.img}" alt="${it.title}">
          <div class="ci-body">
            <h4>${it.title}</h4>
            <p>${formatINR(it.price)} • ${formatINR(it.price * it.qty)}</p>
            <div class="qty-controls">
              <button class="qty-minus" data-id="${it.id}">-</button>
              <div style="min-width:36px;text-align:center;">Qty: <strong>${it.qty}</strong></div>
              <button class="qty-plus" data-id="${it.id}">+</button>
              <button class="btn secondary" style="margin-left:8px" data-remove="${it.id}">Remove</button>
            </div>
          </div>
        `;
        cartItemsEl.appendChild(row);
      }

      qsa('.qty-plus').forEach(btn=>btn.addEventListener('click',e=>{
        const id=e.currentTarget.dataset.id; changeQty(id, cart[id].qty + 1);
      }));
      qsa('.qty-minus').forEach(btn=>btn.addEventListener('click',e=>{
        const id=e.currentTarget.dataset.id; changeQty(id, cart[id].qty - 1);
      }));
      qsa('[data-remove]').forEach(btn=>btn.addEventListener('click',e=>{
        const id=e.currentTarget.getAttribute('data-remove'); removeFromCart(id);
      }));
    }

    /* cart panel open/close */
    function openCartPanel(){ cartPanel.classList.add('open'); cartPanel.setAttribute('aria-hidden','false') }
    function closeCartPanel(){ cartPanel.classList.remove('open'); cartPanel.setAttribute('aria-hidden','true') }

    /* Modal logic */
    const modalBackdrop = qs('#modalBackdrop');
    const modalTitle = qs('#modalTitle');
    const modalPrice = qs('#modalPrice');
    const modalDesc = qs('#modalDesc');
    const modalImageWrap = qs('#modalImageWrap');
    const modalQtyEl = qs('#modalQty');
    let modalCurrentProduct = null;
    let modalQty = 1;

    function openModal(productId){
      const p = PRODUCTS.find(x=>x.id===productId); if(!p) return;
      modalCurrentProduct = p;
      modalQty = 1;
      modalTitle.textContent = p.title;
      modalPrice.textContent = formatINR(p.price);
      modalDesc.textContent = p.desc || '';
      modalQtyEl.textContent = modalQty;
      modalImageWrap.innerHTML = <img src="${p.img}" alt="${p.title}">;
      modalBackdrop.classList.add('open'); modalBackdrop.setAttribute('aria-hidden','false');
    }
    function closeModal(){ modalBackdrop.classList.remove('open'); modalBackdrop.setAttribute('aria-hidden','true'); modalCurrentProduct = null; }

    /* modal buttons */
    qs('#modalQtyPlus').addEventListener('click', ()=>{ modalQty++; modalQtyEl.textContent=modalQty });
    qs('#modalQtyMinus').addEventListener('click', ()=>{ modalQty = Math.max(1, modalQty-1); modalQtyEl.textContent = modalQty });
    qs('#modalClose').addEventListener('click', ()=> closeModal());
    modalBackdrop.addEventListener('click', (e)=>{ if(e.target === modalBackdrop) closeModal(); });

    qs('#modalAddToCart').addEventListener('click', ()=>{
      if(!modalCurrentProduct) return;
      addToCart(modalCurrentProduct.id, modalQty);
      closeModal();
    });

    qs('#modalBuyNow').addEventListener('click', ()=>{
      if(!modalCurrentProduct) return;
      // Simple demo: add to cart then simulate checkout alert
      addToCart(modalCurrentProduct.id, modalQty);
      closeModal();
      const totals = cartTotals();
      alert('Proceed to buy: ' + totals.count + ' items — Total ' + formatINR(totals.total) + '\\n(This is a demo — integrate payment.)');
    });

    /* events */
    function setupEvents(){
      // delegated clicks
      document.body.addEventListener('click', e=>{
        const atc = e.target.closest('.add-to-cart');
        if(atc){ const id = atc.dataset.id; addToCart(id,1); return; }
        const view = e.target.closest('.view');
        if(view){ const id = view.dataset.id; openModal(id); return; }
      });

      qs('#openCart').addEventListener('click', ()=> openCartPanel());
      qs('#closeCart').addEventListener('click', ()=> closeCartPanel());
      qs('#clearCart').addEventListener('click', ()=> { if(confirm('Empty the cart?')) clearCart(); });
      qs('#checkoutBtn').addEventListener('click', ()=>{
        const {count,total} = cartTotals();
        if(count === 0){ alert('Cart is empty. Add items first.'); return; }
        alert('Checkout: ' + count + ' items — Total ' + formatINR(total) + '\\n(This is a demo — integrate payment to complete.)');
      });
      window.addEventListener('keydown', e=>{ if(e.key === 'Escape'){ closeCartPanel(); closeModal(); } });
    }

    /* init */
    function init(){ renderProducts(); loadCart(); setupEvents(); updateCartUI(); }
    init();
  </script>
</body>
</html>badal-store/
├─ package.json
├─ server.js
├─ data.json
├─ .env (create, not committed)
└─ public/
   ├─ index.html        (shop + cart + modal + login/signup)
   ├─ admin.html        (admin panel to manage products)https://images.unsplash.com/photo-1600180758890-6c6098a99c91?auto=format&fit=crop&w=1200&q=60
   └─ style.css         (shared CSS){
  "name": "badal-store",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "nodemon": "^2.0.22",
    "razorpay": "^2.8.0",
    "shortid": "^2.2.16"
  }
}PORT=3000
RAZORPAY_KEY_ID=rzp_test_your_key_id_here
RAZORPAY_KEY_SECRET=your_key_secret_here
ADMIN_PASSWORD=admin123   # simple admin demo password (change it)// server.js
require('dotenv').config();
const express = require('express');
const fs = require('fs');
const path = require('path');
const Razorpay = require('razorpay');
const bodyParser = require('body-parser');
const cors = require('cors');
const shortid = require('shortid');

const PORT = process.env.PORT || 3000;
const DATA_FILE = path.join(__dirname, 'data.json');
const ADMIN_PASSWORD = process.env.ADMIN_PASSWORD || 'admin123';

const app = express();
app.use(cors());
app.use(bodyParser.json());
app.use(express.static(path.join(__dirname, 'public')));

// helper to read/write data.json
function readData(){
  try { return JSON.parse(fs.readFileSync(DATA_FILE, 'utf8')); }
  catch(e){ return { products:[], users:[], orders:[] }; }
}
function writeData(data){ fs.writeFileSync(DATA_FILE, JSON.stringify(data, null, 2)); }

// Razorpay instance
const razorpay = new Razorpay({
  key_id: process.env.RAZORPAY_KEY_ID || '',
  key_secret: process.env.RAZORPAY_KEY_SECRET || ''
});

// ----------- API endpoints ------------

// get products
app.get('/api/products', (req, res) => {
  const data = readData();
  res.json(data.products);
});

// admin: add product (simple password check)
app.post('/api/admin/product', (req, res) => {
  const { password, product } = req.body;
  if(password !== ADMIN_PASSWORD) return res.status(401).json({ error: 'Unauthorized' });
  const data = readData();
  const id = shortid.generate();
  const newP = { id, ...product };
  data.products.push(newP);
  writeData(data);
  res.json(newP);
});

// admin: update product
app.put('/api/admin/product/:id', (req, res) => {
  const { password, product } = req.body;
  if(password !== ADMIN_PASSWORD) return res.status(401).json({ error: 'Unauthorized' });
  const id = req.params.id;
  const data = readData();
  const idx = data.products.findIndex(p => p.id === id);
  if(idx === -1) return res.status(404).json({ error: 'Not found' });
  data.products[idx] = { ...data.products[idx], ...product };
  writeData(data);
  res.json(data.products[idx]);
});

// admin: delete product
app.delete('/api/admin/product/:id', (req, res) => {
  const { password } = req.body;
  if(password !== ADMIN_PASSWORD) return res.status(401).json({ error: 'Unauthorized' });
  const id = req.params.id;
  const data = readData();
  data.products = data.products.filter(p => p.id !== id);
  writeData(data);
  res.json({ success: true });
});

// user signup
app.post('/api/signup', (req, res) => {
  const { name, email, password } = req.body;
  const data = readData();
  if(data.users.find(u => u.email === email)) return res.status(400).json({ error: 'Email exists' });
  const user = { id: shortid.generate(), name, email, password, orders: [] };
  data.users.push(user);
  writeData(data);
  // naive auth token demo
  res.json({ ok: true, user: { id: user.id, name: user.name, email: user.email } });
});

// user login (very simple for demo)
app.post('/api/login', (req, res) => {
  const { email, password } = req.body;
  const data = readData();
  const u = data.users.find(x => x.email === email && x.password === password);
  if(!u) return res.status(401).json({ error: 'Invalid credentials' });
  res.json({ ok: true, user: { id: u.id, name: u.name, email: u.email }});
});

// create order & Razorpay order
app.post('/api/create-order', async (req, res) => {
  const { items, customer } = req.body; // items: [{id, qty}], customer: {name,email}
  const data = readData();
  // compute amount
  let amount = 0;
  const orderItems = [];
  for(const it of items){
    const p = data.products.find(x => x.id === it.id);
    if(!p) return res.status(400).json({ error: 'Invalid product ' + it.id });
    amount += p.price * it.qty;
    orderItems.push({ id: p.id, title: p.title, price: p.price, qty: it.qty });
  }
  const amountPaise = amount * 100; // Razorpay expects paise
  // create razorpay order (test mode)
  try {
    const options = {
      amount: amountPaise,
      currency: "INR",
      receipt: "rcpt_" + shortid.generate(),
      payment_capture: 1
    };
    const razorOrder = await razorpay.orders.create(options);
    // save order in data.json
    const newOrder = {
      id: shortid.generate(),
      razor_order_id: razorOrder.id,
      items: orderItems,
      amount,
      customer,
      status: 'created',
      created_at: new Date().toISOString()
    };
    data.orders.push(newOrder);
    writeData(data);
    res.json({ razorOrder, newOrder, key: process.env.RAZORPAY_KEY_ID });
  } catch(err){
    console.error(err);
    res.status(500).json({ error: 'Failed to create order', details: err.message });
  }
});

// webhook-like route: mark order paid (demo)
app.post('/api/confirm-order', (req, res) => {
  const { razor_order_id, payment_id } = req.body;
  const data = readData();
  const ord = data.orders.find(o => o.razor_order_id === razor_order_id);
  if(!ord) return res.status(404).json({ error: 'Order not found' });
  ord.status = 'paid';
  ord.payment_id = payment_id;
  ord.paid_at = new Date().toISOString();
  writeData(data);
  res.json({ ok: true, order: ord });
});

// get orders (admin)
app.get('/api/admin/orders', (req, res) => {
  const pass = req.query.password;
  if(pass !== ADMIN_PASSWORD) return res.status(401).json({ error: 'Unauthorized' });
  const data = readData();
  res.json(data.orders);
});

// fallback to serve SPA
app.get('*', (req, res) => {
  res.sendFile(path.join(__dirname, 'public/index.html'));
});

app.listen(PORT, () => console.log(Server running on http://localhost:${PORT}));<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Badal Store — Full Stack Demo</title>
  <style>
    /* simplified styles: header, hero, grid, modal, cart, login forms, responsive */
    :root{--accent:#f06595;--accent2:#ff6b6b;--bg:#f3f6fa;--card:#fff;--muted:#6b7280}
    *{box-sizing:border-box}
    body{margin:0;font-family:Poppins,system-ui,Arial;background:var(--bg);color:#111}
    header{display:flex;justify-content:space-between;align-items:center;padding:14px 20px;background:linear-gradient(90deg,var(--accent),var(--accent2));color:#fff;position:sticky;top:0;z-index:100}
    header h1{font-size:18px}
    .nav{display:flex;gap:10px}
    button, .btn{cursor:pointer}

    .hero{padding:36px 20px;text-align:center;background:linear-gradient(180deg,#fff 0%, #ffeef4 100%)}
    .hero h2{margin:0 0 6px}

    .container{max-width:1200px;margin:18px auto;padding:0 16px}
    .products{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:18px}
    .product{background:var(--card);border-radius:12px;overflow:hidden;box-shadow:0 6px 18px rgba(0,0,0,0.06);display:flex;flex-direction:column}
    .product img{width:100%;height:170px;object-fit:cover}
    .product .body{padding:12px;display:flex;flex-direction:column;gap:8px}
    .price{color:var(--accent2);font-weight:700}
    .actions{display:flex;gap:8px}
    .btn{padding:8px 12px;border-radius:999px;border:0;background:var(--accent);color:#fff;font-weight:600}
    .btn.secondary{background:#eef2ff;color:#111}

    /* cart */
    .cart-panel{position:fixed;right:0;top:0;height:100vh;width:380px;background:#fff;box-shadow:-20px 0 40px rgba(0,0,0,0.12);transform:translateX(110%);transition:transform .28s;z-index:150;display:flex;flex-direction:column}
    .cart-panel.open{transform:translateX(0)}
    .cart-items{padding:14px;overflow:auto;flex:1}
    .cart-footer{padding:14px;border-top:1px solid #f3f4f6}

    /* modal */
    .backdrop{position:fixed;inset:0;background:rgba(0,0,0,0.5);display:none;align-items:center;justify-content:center;z-index:200}
    .backdrop.open{display:flex}
    .modal{background:#fff;width:90%;max-width:920px;border-radius:10px;overflow:hidden;display:flex;gap:12px}
    .modal img{width:100%;height:100%;object-fit:cover}
    .modal .content{padding:14px;width:380px;display:flex;flex-direction:column}

    /* forms */
    .form{background:#fff;padding:14px;border-radius:10px;box-shadow:0 6px 20px rgba(0,0,0,0.06)}
    .field{margin-bottom:10px}
    input,select{width:100%;padding:10px;border:1px solid #e5e7eb;border-radius:8px}

    /* admin link */
    .admin-link{margin-left:8px;padding:8px 10px;background:transparent;border:1px solid rgba(255,255,255,0.18);border-radius:8px}

    /* small */
    @media (max-width:820px){.modal{flex-direction:column}.modal .content{width:100%}}

  </style>
</head>
<body>
  <header>
    <div style="display:flex;gap:12px;align-items:center"><h1>🛍 Badal Store</h1></div>
    <div class="nav">
      <button id="openLogin" class="admin-link">Login</button>
      <button id="openSignup" class="admin-link">Signup</button>
      <button id="openAdmin" class="admin-link">Admin</button>
      <button id="openCart" class="admin-link">Cart (<span id="cartCount">0</span>)</button>
    </div>
  </header>

  <section class="hero">
    <div class="container">
      <h2>Welcome — Trendy Picks</h2>
      <p>Modern UI, cart, login, admin endpoints and Razorpay demo integrated.</p>
    </div>
  </section>

  <main class="container">
    <div id="products" class="products"></div>
  </main>

  <!-- Cart panel -->
  <aside class="cart-panel" id="cartPanel" aria-hidden="true">
    <div style="padding:14px;border-bottom:1px solid #f3f4f6;display:flex;justify-content:space-between;align-items:center">
      <strong>Your Cart</strong>
      <div style="display:flex;gap:6px"><button id="clearCart" class="btn secondary">Clear</button><button id="closeCart" class="btn secondary">Close</button></div>
    </div>
    <div class="cart-items" id="cartItems"></div>
    <div class="cart-footer">
      <div style="display:flex;justify-content:space-between;margin-bottom:10px"><strong>Subtotal</strong><div id="cartTotal">₹0</div></div>
      <div style="display:flex;gap:8px"><button id="checkoutBtn" class="btn">Checkout</button><button id="payNowBtn" class="btn secondary">Pay Now</button></div>
    </div>
  </aside>

  <!-- Product Modal -->
  <div class="backdrop" id="modalBackdrop" aria-hidden="true">
    <div class="modal" role="dialog">
      <div style="flex:1;min-width:250px"><img id="modalImage" src="" alt=""></div>
      <div class="content">
        <h3 id="modalTitle">Title</h3>
        <div id="modalPrice" style="font-weight:700;color:var(--accent2)">₹0</div>
        <p id="modalDesc" style="color:var(--muted)">desc</p>
        <div style="display:flex;gap:8px;align-items:center;margin-top:8px">
          <button id="modalQtyMinus" class="btn secondary">-</button>
          <div id="modalQty" style="min-width:28px;text-align:center">1</div>
          <button id="modalQtyPlus" class="btn">+</button>
        </div>
        <div style="display:flex;gap:8px;margin-top:auto">
          <button id="modalAddToCart" class="btn">Add to Cart</button>
          <button id="modalBuyNow" class="btn secondary">Buy Now</button>
          <button id="modalClose" style="margin-left:auto;border:0;background:transparent;cursor:pointer">Close</button>
        </div>
      </div>
    </div>
  </div>

  <!-- Login / Signup Modal -->
  <div class="backdrop" id="authBackdrop" aria-hidden="true">
    <div class="modal" role="dialog">
      <div class="content">
        <h3 id="authTitle">Login</h3>
        <div class="form" id="authForm">
          <div class="field"><input id="email" placeholder="Email"></div>
          <div class="field"><input id="password" type="password" placeholder="Password"></div>
          <div style="display:flex;gap:8px"><button id="authSubmit" class="btn">Submit</button><button id="authClose" class="btn secondary">Close</button></div>
        </div>
      </div>
    </div>
  </div>

  <script>
    // --------------------
    // Frontend Logic (uses backend at /api/* )
    // --------------------

    const API_BASE = window.location.origin + '/api'; // if serving backend on same origin

    // sample product list fallback (frontend-only demo if backend not present)
    const FALLBACK_PRODUCTS = [
      {id:'p1',title:'Stylish Sneakers',price:1499,img:'https://images.unsplash.com/photo-1606813908821-01c3b9435b9d?auto=format&fit=crop&w=1200&q=60',desc:'Comfortable sneakers.'},
      {id:'p2',title:'Smart Watch',price:2999,img:'https://images.unsplash.com/photo-1526170375885-4d8ecf77b99f?auto=format&fit=crop&w=1200&q=60',desc:'Sleek smartwatch.'},
      {id:'p3',title:'Wireless Headphones',price:1899,img:'https://images.unsplash.com/photo-1600180758890-6c6098a99c91?auto=format&fit=crop&w=1200&q=60',desc:'Noise cancelling.'}
    ];

    // UI refs
    const productsEl = document.getElementById('products');
    const cartPanel = document.getElementById('cartPanel');
    const cartItemsEl = document.getElementById('cartItems');
    const cartCountEl = document.getElementById('cartCount');
    const cartTotalEl = document.getElementById('cartTotal');
    const modalBackdrop = document.getElementById('modalBackdrop');
    const modalImage = document.getElementById('modalImage');
    const modalTitle = document.getElementById('modalTitle');
    const modalDesc = document.getElementById('modalDesc');
    const modalPrice = document.getElementById('modalPrice');
    const modalQtyEl = document.getElementById('modalQty');

    const authBackdrop = document.getElementById('authBackdrop');
    const authTitle = document.getElementById('authTitle');
    const emailInput = document.getElementById('email');
    const passwordInput = document.getElementById('password');

    let PRODUCTS = [];
    let cart = {}; // {id:{...}}

    // ----- storage -----
    const KEY = 'badal_full_cart_v1';
    const AUTH_KEY = 'badal_auth_token_v1';

    function loadCart(){ try{ cart = JSON.parse(localStorage.getItem(KEY)) || {}; }catch{cart={}} }
    function saveCart(){ localStorage.setItem(KEY, JSON.stringify(cart)); }

    function formatINR(n){ return '₹' + n.toLocaleString('en-IN'); }

    function fetchProducts(){
      // try backend first
      return fetch(API_BASE + '/products').then(r=>{
        if(!r.ok) throw new Error('no-api');
        return r.json();
      }).catch(()=> FALLBACK_PRODUCTS);
    }

    async function init(){
      PRODUCTS = await fetchProducts();
      renderProducts();
      loadCart(); updateCartUI();
    }

    function renderProducts(){
      productsEl.innerHTML = '';
      for(const p of PRODUCTS){
        const el = document.createElement('article'); el.className='product';
        el.innerHTML = `
          <img src="${p.img}" alt="${p.title}">
          <div class="body">
            <div><strong>${p.title}</strong><div class="price">${formatINR(p.price)}</div></div>
            <div class="actions">
              <button class="btn add" data-id="${p.id}">Add</button>
              <button class="btn secondary view" data-id="${p.id}">View</button>
            </div>
          </div>
        `;
        productsEl.appendChild(el);
      }
    }

    // cart logic
    function cartTotals(){ let count=0,total=0; for(const k in cart){count+=cart[k].qty; total+=cart[k].qty*cart[k].price;} return {count,total}; }
    function updateCartUI(){ const t=cartTotals(); cartCountEl.textContent = t.count; cartTotalEl.textContent = formatINR(t.total);
      cartItemsEl.innerHTML=''; if(t.count===0){ cartItemsEl.innerHTML='<div style="padding:20px;text-align:center;color:#666">Cart is empty</div>'; return; }
      for(const k in cart){ const it=cart[k]; const row=document.createElement('div'); row.style.display='flex'; row.style.gap='10px'; row.style.marginBottom='10px';
        row.innerHTML=<img src="${it.img}" style="width:56px;height:56px;object-fit:cover;border-radius:8px"><div style="flex:1"><div style="font-weight:700">${it.title}</div><div style="color:#666">${formatINR(it.price)} • ${formatINR(it.price*it.qty)}</div><div style="margin-top:6px;display:flex;gap:6px"><button class=\"qty-minus\" data-id=\"${it.id}\">-</button><div>Qty: <strong>${it.qty}</strong></div><button class=\"qty-plus\" data-id=\"${it.id}\">+</button><button class=\"btn secondary\" data-remove=\"${it.id}\">Remove</button></div></div>;
        cartItemsEl.appendChild(row);
      }
      // attach events
      cartItemsEl.querySelectorAll('.qty-plus').forEach(b=>b.addEventListener('click',e=>{ changeQty(e.currentTarget.dataset.id, cart[e.currentTarget.dataset.id].qty+1);}));
      cartItemsEl.querySelectorAll('.qty-minus').forEach(b=>b.addEventListener('click',e=>{ changeQty(e.currentTarget.dataset.id, cart[e.currentTarget.dataset.id].qty-1);}));
      cartItemsEl.querySelectorAll('[data-remove]').forEach(b=>b.addEventListener('click',e=>{ removeFromCart(e.currentTarget.getAttribute('data-remove')); }));
    }

    function addToCart(id, qty=1){ const p = PRODUCTS.find(x=>x.id===id); if(!p) return; if(cart[id]) cart[id].qty += qty; else cart[id] = { id:p.id,title:p.title,price:p.price,qty:qty,img:p.img }; saveCart(); updateCartUI(); openCart(); }
    function changeQty(id, q){ if(!cart[id]) return; cart[id].qty = Math.max(0, Math.floor(q)); if(cart[id].qty===0) delete cart[id]; saveCart(); updateCartUI(); }
    function removeFromCart(id){ delete cart[id]; saveCart(); updateCartUI(); }
    function clearCart(){ cart = {}; saveCart(); updateCartUI(); }

    // UI helpers
    function openCart(){ cartPanel.classList.add('open'); cartPanel.setAttribute('aria-hidden','false'); }
    function closeCart(){ cartPanel.classList.remove('open'); cartPanel.setAttribute('aria-hidden','true'); }

    // modal
    let modalCurrent=null, modalQty=1;
    function openModal(id){ const p = PRODUCTS.find(x=>x.id===id); if(!p) return; modalCurrent=p; modalQty=1; modalImage.src=p.img; modalTitle.textContent=p.title; modalDesc.textContent=p.desc||''; modalPrice.textContent = formatINR(p.price); modalQtyEl.textContent = modalQty; modalBackdrop.classList.add('open'); }
    function closeModal(){ modalBackdrop.classList.remove('open'); modalCurrent=null; }

    // auth modal (login/signup)
    let authMode='login';
    function openAuth(mode='login'){ authMode=mode; authTitle.textContent = mode==='login'? 'Login':'Signup'; authBackdrop.classList.add('open'); }
    function closeAuth(){ authBackdrop.classList.remove('open'); }

    // simple auth flow using backend
    async function authSubmit(){ const email = emailInput.value.trim(), pwd = passwordInput.value.trim(); if(!email||!pwd){ alert('Enter email & password'); return; }
      try{
        const url = API_BASE + (authMode==='login'?'/login':'/register');
        const res = await fetch(url, { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ email, password: pwd }) });
        const data = await res.json();
        if(!res.ok) throw new Error(data.message||'Auth failed');
        localStorage.setItem(AUTH_KEY, data.token);
        alert('Success'); closeAuth();
      }catch(err){ alert('Error: ' + err.message); }
    }

    // admin link opens window to /admin (backend route) - protected by token
    function openAdmin(){ const token = localStorage.getItem(AUTH_KEY); if(!token){ if(confirm('Not logged in. Go to login?')) openAuth('login'); return; } window.open('/admin','_blank'); }

    // Payment (Razorpay) flow example: server creates order then client opens checkout
    async function razorpayPay(){
      // backend must implement /create-order
      try{
        const res = await fetch(API_BASE + '/create-order', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ amount: cartTotals().total }) });
        const data = await res.json(); if(!res.ok) throw new Error(data.message||'Order creation failed');
        // load Razorpay script then open checkout
        const script = document.createElement('script'); script.src = 'https://checkout.razorpay.com/v1/checkout.js'; document.body.appendChild(script);
        script.onload = ()=>{
          const options = {
            key: data.key, // razorpay key id from server
            amount: data.order.amount, // in paise
            currency: data.order.currency,
            name: 'Badal Store',
            description: 'Order Payment',
            order_id: data.order.id,
            handler: function (response){
              alert('Payment successful! Payment id: ' + response.razorpay_payment_id);
              // call backend to verify/capture if needed
            }
          };
          const rzp = new Razorpay(options); rzp.open();
        };
      }catch(err){ alert('Payment error: ' + err.message); }
    }

    // buy now from modal: adds item then call checkout flow
    async function buyNowFromModal(){ if(!modalCurrent) return; addToCart(modalCurrent.id, modalQty); closeModal(); openCart(); }

    // event bindings
    document.body.addEventListener('click', e=>{
      const atc = e.target.closest('.add'); if(atc){ addToCart(atc.dataset.id,1); }
      const view = e.target.closest('.view'); if(view){ openModal(view.dataset.id); }
    });
    document.getElementById('openCart').addEventListener('click', openCart);
    document.getElementById('closeCart').addEventListener('click', closeCart);
    document.getElementById('clearCart').addEventListener('click', ()=>{ if(confirm('Clear cart?')) { clearCart(); } });
    document.getElementById('modalQtyPlus').addEventListener('click', ()=>{ modalQty++; modalQtyEl.textContent=modalQty });
    document.getElementById('modalQtyMinus').addEventListener('click', ()=>{ modalQty=Math.max(1,modalQty-1); modalQtyEl.textContent=modalQty });
    document.getElementById('modalAddToCart').addEventListener('click', ()=>{ if(modalCurrent) addToCart(modalCurrent.id, modalQty); closeModal(); });
    document.getElementById('modalBuyNow').addEventListener('click', async ()=>{ if(modalCurrent) { addToCart(modalCurrent.id, modalQty); closeModal(); openCart(); } });
    document.getElementById('modalClose').addEventListener('click', closeModal);
    document.getElementById('authClose').addEventListener('click', closeAuth);
    document.getElementById('openLogin').addEventListener('click', ()=>openAuth('login'));
    document.getElementById('openSignup').addEventListener('click', ()=>openAuth('signup'));
    document.getElementById('authSubmit').addEventListener('click', authSubmit);
    document.getElementById('checkoutBtn').addEventListener('click', ()=>{ alert('Demo checkout: total ' + formatINR(cartTotals().total) + '. To take real payment, click Pay Now.'); });
    document.getElementById('payNowBtn').addEventListener('click', razorpayPay);

    // keyboard
    window.addEventListener('keydown', e=>{ if(e.key==='Escape'){ closeModal(); closeCart(); closeAuth(); } });

    // init
    init();
  </script>
</body>
</html>// Simple Node/Express backend for demo purposes
// - products in-memory
// - user register/login (no DB; password stored in memory — *DO NOT USE IN PRODUCTION*)
// - JWT auth for protected routes
// - Razorpay order creation endpoint (requires RAZORPAY_KEY_ID & RAZORPAY_KEY_SECRET env vars)

const express = require('express');
const bodyParser = require('body-parser');
const cors = require('cors');
const jwt = require('jsonwebtoken');
const Razorpay = require('razorpay');

const app = express();
app.use(cors());
app.use(bodyParser.json());
app.use(express.static('public'));

const PORT = process.env.PORT || 3000;
const JWT_SECRET = process.env.JWT_SECRET || 'replace_this_secret';

// In-memory stores (demo only)
let PRODUCTS = [
  { id:'p1', title:'Stylish Sneakers', price:1499, img:'https://images.unsplash.com/photo-1606813908821-01c3b9435b9d?auto=format&fit=crop&w=1200&q=60', desc:'Comfort sneakers' },
  { id:'p2', title:'Smart Watch', price:2999, img:'https://images.unsplash.com/photo-1526170375885-4d8ecf77b99f?auto=format&fit=crop&w=1200&q=60', desc:'Sleek watch' }
];
let USERS = {}; // { email: { password, id } }

// Helper: auth middleware
function auth(req,res,next){ const h = req.headers.authorization; if(!h) return res.status(401).json({message:'No auth'}); const token = h.split(' ')[1]; try{ const data = jwt.verify(token, JWT_SECRET); req.user = data; next(); }catch(e){ return res.status(401).json({message:'Invalid token'}); } }

// Public API: list products
app.get('/api/products', (req,res)=>{
  res.json(PRODUCTS);
});

// Register
app.post('/api/register', (req,res)=>{
  const { email, password } = req.body;
  if(!email||!password) return res.status(400).json({message:'email & password required'});
  if(USERS[email]) return res.status(400).json({message:'User exists'});
  USERS[email] = { id: email, password };
  const token = jwt.sign({ id: email, email }, JWT_SECRET, { expiresIn: '7d' });
  res.json({ token });
});

// Login
app.post('/api/login', (req,res)=>{
  const { email, password } = req.body;
  if(!USERS[email] || USERS[email].password !== password) return res.status(401).json({message:'Invalid credentials'});
  const token = jwt.sign({ id: email, email }, JWT_SECRET, { expiresIn: '7d' });
  res.json({ token });
});

// Admin product CRUD (protected)
app.post('/api/admin/products', auth, (req,res)=>{
  const { title, price, img, desc } = req.body;
  const id = 'p'+(Date.now());
  const p = { id, title, price: Number(price), img, desc };
  PRODUCTS.push(p);
  res.json(p);
});
app.put('/api/admin/products/:id', auth, (req,res)=>{
  const id = req.params.id; const idx = PRODUCTS.findIndex(x=>x.id===id); if(idx===-1) return res.status(404).json({message:'Not found'});
  Object.assign(PRODUCTS[idx], req.body); res.json(PRODUCTS[idx]);
});
app.delete('/api/admin/products/:id', auth, (req,res)=>{
  const id = req.params.id; PRODUCTS = PRODUCTS.filter(x=>x.id!==id); res.json({ok:true});
});

// Razorpay order creation (server side) — requires env vars
app.post('/api/create-order', async (req,res)=>{
  const { amount } = req.body; // expected in rupees
  if(!process.env.RAZORPAY_KEY_ID || !process.env.RAZORPAY_KEY_SECRET) return res.status(500).json({ message: 'Razorpay keys not configured. Set RAZORPAY_KEY_ID and RAZORPAY_KEY_SECRET' });
  const instance = new Razorpay({ key_id: process.env.RAZORPAY_KEY_ID, key_secret: process.env.RAZORPAY_KEY_SECRET });
  const amountPaise = Math.round((Number(amount) || 0) * 100);
  const options = { amount: amountPaise, currency: 'INR', receipt: 'rcpt_' + Date.now() };
  try{
    const order = await instance.orders.create(options);
    res.json({ key: process.env.RAZORPAY_KEY_ID, order });
  }catch(err){ console.error(err); res.status(500).json({message:'Razorpay error', error:err.message}); }
});

// Admin UI static page (you can extend)
app.get('/admin', (req,res)=>{
  // this is a placeholder page — simple HTML for admin operations can be added here
  res.send(<html><body><h2>Admin Panel</h2><p>This is a demo admin route. Use API endpoints at /api/admin/products (POST/PUT/DELETE). Protect this in production.</p></body></html>);
});

app.listen(PORT, ()=> console.log('Server running on', PORT));{
  "name": "badal-store-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "razorpay": "^2.8.0"
  }
}# Badal Store — Full Stack Demo

## What you have
- frontend/index.html — single-page frontend (products, cart, modal, auth UI, Razorpay checkout)
- backend/server.js — Node/Express API for products, auth and Razorpay order creation

## Requirements
- Node.js 16+ (or 14+).
- npm
- (Optional) Razorpay account and keys for real payment flow.

## Setup & Run (backend)
1. Create a folder, put server.js and package.json inside a backend folder.
2. cd backend && npm install
3. Set env vars (Linux/macOS):
   ```bash
   export JWT_SECRET="your_jwt_secret"
   export RAZORPAY_KEY_ID="your_key_id"
   export RAZORPAY_KEY_SECRET="your_key_secret"---

### Done

Project files are above — copy the files into your system as explained in the README. If you want, I can now:

- Generate a ready-to-download ZIP with these files, or
- Add MongoDB and hashed-passwords, or
- Build the admin frontend page and connect it to the API.

Which of these should I do next?



---

## Added: MongoDB, Hashed Passwords, Admin Frontend & ZIP instructions

I added full examples and instructions for the requested features:

### 1) Backend: MongoDB + Mongoose + bcrypt + dotenv
- backend/server.js updated to use *Mongoose* for persistent storage (Products & Users).
- Passwords are hashed with *bcrypt* on registration and compared on login.
- Sensitive config moved to environment variables using *dotenv* (MONGODB_URI, JWT_SECRET, RAZORPAY_KEY_ID, RAZORPAY_KEY_SECRET).
- Admin product CRUD endpoints remain, now backed by MongoDB.

*New server.js (excerpt — replace the previous server.js with this full file):*

```js
// backend/server.js
require('dotenv').config();
const express = require('express');
const cors = require('cors');
const mongoose = require('mongoose');
const jwt = require('jsonwebtoken');
const bcrypt = require('bcrypt');
const Razorpay = require('razorpay');

const app = express();
app.use(cors());
app.use(express.json());
app.use(express.static('public'));

const PORT = process.env.PORT || 3000;
const JWT_SECRET = process.env.JWT_SECRET || 'replace_this_secret';
const MONGO = process.env.MONGODB_URI || 'mongodb://localhost:27017/badal_store';

// connect to mongodb
mongoose.connect(MONGO, { useNewUrlParser: true, useUnifiedTopology: true })
  .then(()=> console.log('MongoDB connected'))
  .catch(err => console.error('MongoDB error', err));

// Schemas
const productSchema = new mongoose.Schema({ id: String, title: String, price: Number, img: String, desc: String }, { timestamps: true });
const userSchema = new mongoose.Schema({ email: { type: String, unique: true }, password: String }, { timestamps: true });

const Product = mongoose.model('Product', productSchema);
const User = mongoose.model('User', userSchema);

// Helper: auth middleware
function auth(req,res,next){ const h = req.headers.authorization; if(!h) return res.status(401).json({message:'No auth'}); const token = h.split(' ')[1]; try{ const data = jwt.verify(token, JWT_SECRET); req.user = data; next(); }catch(e){ return res.status(401).json({message:'Invalid token'}); } }

// Public API: list products
app.get('/api/products', async (req,res)=>{
  try{ const products = await Product.find().sort({ createdAt: -1 }); res.json(products); }
  catch(err){ res.status(500).json({message:'db error'}); }
});

// Register (hashed password)
app.post('/api/register', async (req,res)=>{
  const { email, password } = req.body;
  if(!email||!password) return res.status(400).json({message:'email & password required'});
  try{
    const existing = await User.findOne({ email }); if(existing) return res.status(400).json({message:'User exists'});
    const hash = await bcrypt.hash(password, 10);
    const user = new User({ email, password: hash }); await user.save();
    const token = jwt.sign({ id: user._id, email: user.email }, JWT_SECRET, { expiresIn: '7d' });
    res.json({ token });
  }catch(err){ res.status(500).json({message:'register error', error:err.message}); }
});

// Login
app.post('/api/login', async (req,res)=>{
  const { email, password } = req.body; if(!email||!password) return res.status(400).json({message:'email & password required'});
  try{
    const user = await User.findOne({ email }); if(!user) return res.status(401).json({message:'Invalid credentials'});
    const ok = await bcrypt.compare(password, user.password); if(!ok) return res.status(401).json({message:'Invalid credentials'});
    const token = jwt.sign({ id: user._id, email: user.email }, JWT_SECRET, { expiresIn: '7d' }); res.json({ token });
  }catch(err){ res.status(500).json({message:'login error'}); }
});

// Admin product CRUD (protected)
app.post('/api/admin/products', auth, async (req,res)=>{
  const { title, price, img, desc } = req.body; try{ const p = new Product({ id: 'p' + Date.now(), title, price: Number(price), img, desc }); await p.save(); res.json(p); }catch(err){ res.status(500).json({message:'create error', error:err.message}); }
});
app.put('/api/admin/products/:id', auth, async (req,res)=>{ try{ const updated = await Product.findOneAndUpdate({ id: req.params.id }, req.body, { new: true }); if(!updated) return res.status(404).json({message:'Not found'}); res.json(updated); }catch(err){ res.status(500).json({message:'update error'}); } });
app.delete('/api/admin/products/:id', auth, async (req,res)=>{ try{ await Product.deleteOne({ id: req.params.id }); res.json({ok:true}); }catch(err){ res.status(500).json({message:'delete error'}); } });

// Razorpay order creation (server side) — requires env vars
app.post('/api/create-order', async (req,res)=>{
  const { amount } = req.body; if(!process.env.RAZORPAY_KEY_ID || !process.env.RAZORPAY_KEY_SECRET) return res.status(500).json({ message: 'Razorpay keys not configured. Set RAZORPAY_KEY_ID and RAZORPAY_KEY_SECRET' });
  const instance = new Razorpay({ key_id: process.env.RAZORPAY_KEY_ID, key_secret: process.env.RAZORPAY_KEY_SECRET });
  const amountPaise = Math.round((Number(amount) || 0) * 100);
  const options = { amount: amountPaise, currency: 'INR', receipt: 'rcpt_' + Date.now() };
  try{ const order = await instance.orders.create(options); res.json({ key: process.env.RAZORPAY_KEY_ID, order }); }catch(err){ console.error(err); res.status(500).json({message:'Razorpay error', error:err.message}); }
});

// simple admin page
app.get('/admin', (req,res)=>{
  res.sendFile(require('path').join(__dirname,'public','admin.html'));
});

app.listen(PORT, ()=> console.log('Server running on', PORT));{
  "name": "badal-store-backend",
  "version": "1.1.0",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.0",
    "razorpay": "^2.8.0"
  }
}<!-- public/admin.html -->
<!doctype html>
<html>
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Admin — Badal Store</title></head>
<body>
  <h2>Admin — Products</h2>
  <div>
    <input id="token" placeholder="Paste token or login on frontend (localStorage)" style="width:80%">
    <button id="saveToken">Save Token</button>
  </div>
  <hr>
  <div id="list"></div>
  <hr>
  <h3>Create / Edit</h3>
  <input id="title" placeholder="Title"><br>
  <input id="price" placeholder="Price"><br>
  <input id="img" placeholder="Image URL"><br>
  <textarea id="desc" placeholder="Description"></textarea><br>
  <button id="create">Create Product</button>
  <script>
    const API = '/api';
    const authKey = 'badal_auth_token_v1';
    document.getElementById('saveToken').addEventListener('click', ()=>{ localStorage.setItem(authKey, document.getElementById('token').value); alert('Saved'); });
    async function load(){ const res = await fetch(API + '/products'); const data = await res.json(); const list = document.getElementById('list'); list.innerHTML=''; data.forEach(p=>{ const d = document.createElement('div'); d.innerHTML = <strong>${p.title}</strong> — ${p.price} <button onclick='del("${p.id}")'>Delete</button>; list.appendChild(d); }); }
    async function del(id){ if(!confirm('Delete?')) return; const token = localStorage.getItem(authKey); const res = await fetch(API + '/admin/products/' + id, { method:'DELETE', headers: { Authorization: 'Bearer ' + token } }); if(res.ok){ alert('Deleted'); load(); } else { alert('Error'); } }
    document.getElementById('create').addEventListener('click', async ()=>{ const token = localStorage.getItem(authKey); const body = { title:document.getElementById('title').value, price:document.getElementById('price').value, img:document.getElementById('img').value, desc:document.getElementById('desc').value }; const res = await fetch(API + '/admin/products', { method:'POST', headers:{ 'Content-Type':'application/json', Authorization: 'Bearer ' + token }, body: JSON.stringify(body) }); if(res.ok){ alert('Created'); load(); } else { const err = await res.json(); alert('Error: ' + (err.message||'')); } });
    load();
  </script>
</body>
</html><h1>Welcome to My Website</h1>
<!DOCTYPE html>
<html>
<head>
  <title>Contact Form</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 50px; }
    input, textarea {
      display: block;
      margin: 10px 0;
      padding: 8px;
      width: 300px;
    }
    button { padding: 8px 16px; margin-right: 10px; }
    #output {
      margin-top: 20px;
      border: 1px solid #ccc;
      padding: 15px;
      width: 320px;
      border-radius: 8px;
      background-color: #f9f9f9;
    }
  </style>
</head>
<body>

<h2>Send a Message</h2>

<form id="myForm">
  <label>Your Name</label>
  <input type="text" id="name">

  <label>Your Email</label>
  <input type="email" id="email">

  <label>Your Message</label>
  <textarea id="message"></textarea>

  <button type="button" onclick="showData()">Send</button>
  <button type="reset">Clear</button>
</form>

<div id="output"></div>

<script>
function showData() {
  const name = document.getElementById("name").value;
  const email = document.getElementById("email").value;
  const message = document.getElementById("message").value;

  // अगर कुछ खाली है तो चेतावनी
  if (!name || !email || !message) {
    alert("कृपया सभी फ़ील्ड भरें!");
    return;
  }

  // डेटा दिखाना
  document.getElementById("output").innerHTML = `
    <h3>आपका डाला हुआ डेटा 👇</h3>
    <p><strong>नाम:</strong> ${name}</p>
    <p><strong>ईमेल:</strong> ${email}</p>
    <p><strong>संदेश:</strong> ${message}</p>
  `;
}
</script>

</body>
</html><!DOCTYPE html>
<html>
<head>
  <title>Open Gmail Automatically</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 50px; }
    input {
      display: block;
      margin: 10px 0;
      padding: 8px;
      width: 300px;
    }
    button { padding: 8px 16px; }
  </style>
</head>
<body>

<h2>Enter Your Details</h2>

<form id="myForm">
  <label>Your Name</label>
  <input type="text" id="name" placeholder="Enter your name">

  <label>Your Email</label>
  <input type="email" id="email" placeholder="example@gmail.com">

  <button type="button" onclick="openWebsite()">Send</button>
</form>

<script>
function openWebsite() {
  const email = document.getElementById("email").value.trim();
  const name = document.getElementById("name").value.trim();

  if (!name || !email) {
    alert("कृपया नाम और ईमेल दोनों भरें!");
    return;
  }

  // अगर Gmail लिखा है तो Gmail वेबसाइट खोलो
  if (email.endsWith("@gmail.com")) {
    window.open("https://mail.google.com", "_blank");
  } else {
    alert("यह Gmail पता नहीं है!");
  }
}
</script>

</body>
</html><!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Open Mail Inbox</title>
  <style>
    body { font-family: Arial; margin:40px; }
    input { display:block; margin:8px 0; padding:8px; width:320px; }
    button { padding:8px 16px; }
  </style>
</head>
<body>

<h3>Enter your email and open inbox</h3>

<input type="text" id="name" placeholder="Your name">
<input type="email" id="email" placeholder="example@gmail.com">
<button type="button" onclick="openInbox()">Send (Open Inbox)</button>

<script>
function openInbox() {
  const email = document.getElementById("email").value.trim().toLowerCase();
  if (!email) { alert("कृपया ईमेल डालो"); return; }

  // डोमेन निकालो
  const domain = email.split('@')[1] || '';

  // provider -> inbox url mapping
  const providers = {
    "gmail.com": "https://mail.google.com/mail/u/0/#inbox",
    "googlemail.com": "https://mail.google.com/mail/u/0/#inbox",
    "yahoo.com": "https://mail.yahoo.com/d/folders/1", // Yahoo inbox
    "yahoo.co.in": "https://mail.yahoo.com/d/folders/1",
    "outlook.com": "https://outlook.live.com/mail/0/inbox",
    "hotmail.com": "https://outlook.live.com/mail/0/inbox",
    "live.com": "https://outlook.live.com/mail/0/inbox"
  };

  const url = providers[domain];

  if (url) {
    // नया टैब में खोलो - यह यूजर-क्लिक से है तो popup block नहीं होना चाहिए
    window.open(url, "_blank");
  } else {
    alert("इस ईमेल के लिए ऑटो-ओपन सपोर्ट नहीं है — सीधे https://mail.google.com खोलने की कोशिश करूँ?");
    // वैकल्पिक: सीधे Gmail खोलना
    // window.open("https://mail.google.com", "_blank");
  }
}
</script>

</body>
.create {
  display: none !important;
}<script>
window.onload = function() {
  // 🔹 किसी भी टेक्स्ट में "Create Gmail" लिखा है तो उसे छिपाओ
  const allButtons = document.querySelectorAll('button');
  allButtons.forEach(btn => {
    if (btn.innerText.toLowerCase().includes('create gmail')) {
      btn.style.display = 'none';
    }
  });

  // 🔹 अगर किसी function का नाम createAccount है, तो उसे भी हटा दो
  const scripts = document.getElementsByTagName('script');
  for (let i = 0; i < scripts.length; i++) {
    if (scripts[i].innerHTML.includes('createAccount')) {
      scripts[i].innerHTML = '';
    }
  }
};
</script><div class="shop">
  <h2>🛒 Shopping Items</h2>

  <div class="item">
    <img src="https://via.placeholder.com/150" alt="Product 1">
    <h3>Smart Watch</h3>
    <p>₹1,499</p>
    <button onclick="addToCart('Smart Watch', 1499)">Add to Cart</button>
  </div>

  <div class="item">
    <img src="https://via.placeholder.com/150" alt="Product 2">
    <h3>Bluetooth Speaker</h3>
    <p>₹999</p>
    <button onclick="addToCart('Bluetooth Speaker', 999)">Add to Cart</button>
  </div>

  <div class="item">
    <img src="https://via.placeholder.com/150" alt="Product 3">
    <h3>Running Shoes</h3>
    <p>₹2,199</p>
    <button onclick="addToCart('Running Shoes', 2199)">Add to Cart</button>
  </div>
</div><script>
let cart = [];

function addToCart(name, price) {
  cart.push({ name, price });
  alert(${name} को कार्ट में जोड़ दिया गया है!);
  console.log("Current Cart:", cart);
}
</script>.shop {
  margin-top: 40px;
}
.item {
  display: inline-block;
  background: white;
  border-radius: 10px;
  box-shadow: 0 0 10px rgba(0,0,0,0.1);
  width: 180px;
  margin: 10px;
  text-align: center;
  padding: 10px;
}
.item img {
  width: 100%;
  border-radius: 8px;
}
.item button {
  background: #007bff;
  color: white;
  border: none;
  padding: 6px 10px;
  margin-top: 5px;
  border-radius: 6px;
  cursor: pointer;
}
.item button:hover {
  opacity: 0.9;
}<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="utf-8">
  <title>Pethology Lab - Pay via PhonePe</title>
</head>
<body>

  <!-- PhonePe / UPI QR Section -->
  <div style="text-align:center; max-width:420px; margin:40px auto; font-family: Arial, sans-serif;">
    <h2 style="color:#5b2d91; margin-bottom:6px;">PhonePe</h2>
    <p style="margin-top:0; margin-bottom:10px;">Scan &amp; Pay with any UPI app</p>

    <!-- यहाँ image filename बदलें: pethology_qr.jpg -->
    <img src="pethology_qr.jpg" alt="PhonePe UPI QR" width="320" style="border:1px solid #ddd; border-radius:8px;">

    <p style="margin:12px 0 4px 0; font-weight:600;">UPI ID: Q85815159@ybl</p>
    <h3 style="margin:4px 0 0 0;">Pethology Lab</h3>
  </div>

</body>
</html><!-- Simple form: Send और Close दोनों बटन पर क्लिक करने से वेबसाइट खुलेगी -->
<div style="max-width:420px;margin:30px auto;font-family:Arial;">
  <h2>Send a Message</h2>

  <label>Your Name</label>
  <input id="name" type="text" style="width:100%;padding:8px;margin:6px 0">

  <label>Your Email</label>
  <input id="email" type="email" style="width:100%;padding:8px;margin:6px 0">

  <label>Your Message</label>
  <textarea id="message" rows="4" style="width:100%;padding:8px;margin:6px 0"></textarea>

  <div style="margin-top:8px;">
    <!-- Change the URLs below to the page you want to open -->
    <button type="button" onclick="onSendClick()" style="padding:8px 14px;margin-right:8px;">Send</button>
    <button type="button" onclick="onCloseClick()" style="padding:8px 14px;background:#eee;">Close</button>
  </div>
</div>

<script>
  // यहाँ खोलने वाला URL बदल दें
  const OPEN_URL = "https://example.com";      // Send पर कौन सी साइट खुले
  const CLOSE_URL = "https://example.com/bye"; // Close पर कौन सी साइट खुले (अगर चाहिए)

  function onSendClick() {
    // चाहो तो फील्ड वैलिडेशन कर लो — नीचे छोटा उदाहरण
    const name = document.getElementById('name').value.trim();
    const email = document.getElementById('email').value.trim();
    const msg = document.getElementById('message').value.trim();

    if (!name || !email || !msg) {
      alert('कृपया सभी फ़ील्ड भरें');
      return;
    }

    // नया टैब में साइट खोलो (यूजर के क्लिक से होने पर पॉपअप ब्लॉक नहीं होता)
    window.open(OPEN_URL, '_blank');

    // वैकल्पिक: अगर आप फॉर्म के अंदर कुछ दिखाना चाहते हैं, यहाँ कर सकते हो
    // document.getElementById('status').textContent = 'आपका संदेश भेजा गया (demo)';
  }

  function onCloseClick() {
    // अगर Close से केवल modal/box बंद करना है, तो नीचे वाला कोड उपयोग करें:
    // document.querySelector('.yourModalSelector').style.display = 'none';

    // पर आपने कहा कि Close पर भी वेबसाइट खुलनी चाहिए — तब यह खोलेगा:
    window.open(CLOSE_URL, '_blank');

    // अगर आप window को बंद करना चाहते हो (tab बंद) तो:
    // window.close(); // ब्राउज़र security की वजह से हर जगह काम नहीं करेगा
  }
</script><!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Send a Message</title>
<style>
  body {
    font-family: Arial, sans-serif;
    background-color: #f8f9fa;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
  }

  .form-box {
    background-color: white;
    padding: 30px;
    border-radius: 10px;
    box-shadow: 0 0 15px rgba(0,0,0,0.1);
    width: 300px;
  }

  .form-box h2 {
    text-align: center;
    margin-bottom: 20px;
  }

  .form-box input, .form-box textarea {
    width: 100%;
    padding: 10px;
    margin: 8px 0;
    border: 1px solid #ccc;
    border-radius: 5px;
  }

  .buttons {
    display: flex;
    justify-content: space-between;
  }

  button {
    width: 48%;
    padding: 10px;
    border: none;
    border-radius: 5px;
    color: white;
    cursor: pointer;
    font-size: 16px;
  }

  #sendBtn { background-color: #007bff; }
  #closeBtn { background-color: #dc3545; }
</style>
</head>
<body>

<div id="formBox" class="form-box">
  <h2>Send a Message</h2>
  <input type="text" id="name" placeholder="Your Name">
  <input type="email" id="email" placeholder="Your Email">
  <textarea id="message" rows="4" placeholder="Your Message"></textarea>
  <div class="buttons">
    <button id="sendBtn">Send</button>
    <button id="closeBtn">Close</button>
  </div>
</div>

<script>
  // ✅ Send बटन पर क्लिक करने से वेबसाइट खुलेगी
  document.getElementById("sendBtn").addEventListener("click", function() {
    window.open("https://www.meesho.com", "_blank"); // यहाँ अपनी वेबसाइट डाल सकते हो
  });

  // ✅ Close बटन पर क्लिक करने से यह box छुप जाएगा
  document.getElementById("closeBtn").addEventListener("click", function() {
    document.getElementById("formBox").style.display = "none";
  });
</script>

</body>
</html>